# 实验记录补遗

> 性质：**正文的补遗与勘误**，不是新的方案推荐。  
> 收录：正文没有单列、但在截止线之前真实发生过的模型运行、离线审计、执行中止与后验复核。  
> 模板与正文一致：`要回答什么问题 → 怎么做的 → 结果 → 否决/修正了什么 → 这个结论有多弱`。  
> 若本补遗的后验审计削弱了正文的旧解释，**以较弱、较晚、可复核的表述为准**。

---

## 0. 为什么会漏掉这些

正文是从**已归档的设计文档**整理的。归档文档只保留收口后的结论，于是有三类东西被系统性地丢掉：

| 被丢掉的 | 为什么它有价值 |
|---|---|
| **被否决候选的中间运行** | 它们曾经跑出过**强正向**结果。只看最终处置，会以为这条路从头到尾都不行 |
| **执行中止** | 一次 HTTP 400、一次 schema 拒绝，看起来像「实验失败」；实际上**语义层根本还没被测到** |
| **「这个数是在什么条件下测的」** | 分数会被归档，条件不会 |

第三类最危险，它直接产生**归因错误**——本补遗的 S9 与 S11 都是这一类。

每项另标状态：

```text
真实模型运行 · 离线审计/复算 · 执行中止 · 后验有效性审计
```

---

## 1. 总表

| # | 名称 | 类型 | 规模 | 一句话结果 |
|---|---|---|---|---|
| **S1** | 候选一的合成选拔与判定层隔离 | 真实模型 | 30 次调用 + 40 个成员判断 | 合成集上从 0.64 到 1.00，但只获「暂选」 |
| **S2** | 切分输入单元的外部盲注校准 | 盲注 / 离线 | 30 个合成案例 | 主投影 30/30；元数据大量分歧，**不是人类上限** |
| **S3** | 候选二的两次中间诊断 | 真实模型 | 8 + 16 次调用 | 小集连续改善、零回归；扩到 12 卡只到 10/12 |
| **S4** | 「组件优先」的合同恢复考古 | 真实模型 + 离线 | 六个阶段 | **前四次都死在合同层**；闭合后才暴露语义失败 |
| **S5** | 私有拒绝原因的零调用审计 | 离线审计 | provider 0 次 | 缺的不是整理脚本，是**原合同没要求产生那种证据** |
| **S6** | 所指账本的容量阶梯 | 真实模型 + 离线 | 五级阶梯 | **表示层不再报错，语义问题才第一次被看见** |
| **S7** | 首次机器样本前的合同审计 | 离线工程审计 | 11 个阻塞项 | 「能跑」之前主链有七类缺口；修到可跑 ≠ 效果已证实 |
| **S8** | fail-close 完整性离线复查 | 离线复算 | 20 份已存响应 | 局部坏成分不再抹掉整条已验证上下文 |
| **S9** | 关系判定两轮的后验有效性审计 | 后验审计 | 代码 + 提示版本时间线 | **本补遗最重要的勘误**：旧归因必须降级 |
| **S10** | 线索消融：标签 / 词面 / 向量 / 话题的五种组合 | 真实模型 + 离线 | 五轮 | **精确的都稀疏，能提高覆盖的噪声都比信号多** |
| **S11** | 两条横向观察（维护者） | 观察 | 未量化 | 两两对比的代价；感想类文本上**精确字段可能强过话题向量** |

---

# 2. S1 · 候选一的合成选拔与判定层隔离

**真实模型运行。** 正文只保留了「候选一未冻结」这个最终处置，本节补回它曾经跑出过的强正向结果。

### 要回答的问题

两个，其中第二个是刻意做的隔离：

1. 一个明确区分「容器」与「具体共同核心」的候选，能否在**全合成材料**上明显改善？
2. **如果把正确的共同核心当作 oracle 直接给到第二阶段**，成员角色判断本身是否仍有独立的大缺陷？

第二问的作用是**堵住一条解释**：否则「第一阶段判错」与「第二阶段判错」会互相甩锅。

### 怎么做的

中性合成卡，不接触真实记忆；旧提示词与候选一**成对交错**运行；第二阶段绕过第一阶段、直接喂 oracle 锚点。共 **30 次真实调用**，第二阶段累计 **40 个成员级判断**。

### 结果

**第一阶段：**

| 指标 | 旧提示词 | 候选一 |
|---|---:|---:|
| 决策正确率 | 0.64 | **1.00** |
| 锚点精确率 | 0.50 | **1.00** |
| 弃权召回 | 0.43 | **1.00** |
| 过度弃权 | 0 | 0 |
| 宽泛锚点 | 3 | **1** |
| 关系容器误判 | 2 | **0** |
| 结构化闸门 通过/表层/失败 | 6 / 1 / 4 | **10 / 1 / 0** |

**第二阶段（oracle 锚点下）：** 40 个成员判断，正确 36（0.90），难例 0.87，**严重翻转 0**。

机械处置：**暂选，仅限该合成边界内。**

### 否决 / 修正了什么

**① 它没有证明候选一可用**，只证明了两件更窄的事：容器误判**并非不可动**（在刻意构造的陷阱上，改提示词能显著改变行为）；以及在 oracle 条件下第二阶段没有同等规模的独立灾难——**因此「问题全在成员角色层」这个解释不成立**。

**② 否决了「看到一个漂亮的合成分数就冻结提示词」。** 1.00 出现在一个很小的有界集上，后续仍要求完整的联合实验。

### 限制

全合成；场景与提示由同一条实验线设计；**这是候选筛选证据，不是泛化证据**。

---

# 3. S2 · 切分输入单元的外部盲注校准

**盲注 / 离线。** 正文 E4 为防误读把这条线整体降格成「格式与管道校准」；这里补回其中一轮盲注的过程。

### 要回答的问题

刻意不问「人类是否都这么切」，只问一个更窄的：**已冻结的主输入单元规则，会不会在一个没见过精确答案的外部标注者手里产生严重歧义？**

### 怎么做的

30 个合成案例；盲注者独立作答；**只比较四个会直接影响下一阶段输入的维度**——决策类别、主归属、接受的边界、联合实验资格。附属元数据（候选边界、上下文跨度、替代方案、时间尺度词表）**不并入主一致率**。

### 结果

四个主维度**全部 30/30**。

但附属元数据完全不是这样：

| 差异项 | 案例数 |
|---|---:|
| 不确定的候选边界 | 6 |
| 上下文跨度 | 7 |
| 替代方案 | 3 |
| **时间尺度词表** | **15** |
| 仅参考侧可见的非观察性标注 | 3 |

### 否决 / 修正了什么

**① 否决把 30/30 读成「人类切分一致率 100%」。** 盲注者不是独立人类样本，这一轮也不是人类上限实验。

**② 支持把「主输入单元」与「所有解释元数据」拆开。** 若要求全部字段一起一致，管道会被大量**不影响输入单元**的词表差异卡住——时间尺度词表一项就有 15/30 不一致。

### 限制

全合成；**单个**外部盲注者。30/30 只说明这份最小投影在该盲注者处没有产生分歧。

---

# 4. S3 · 候选二的两次中间诊断

**真实模型运行。** 正文 E3 已记录完整 12 卡的最终 10/12，本节补回在那之前被压掉的两次。

### 要回答的问题

候选二只做一个很窄的改动：**共享人物、组织、地点、关系、主题或任务只能作为容器，不能单独构成稳定核心。**

它是真的在修容器误报，还是顺手破坏了正常正例？

### 怎么做的

两次有界诊断：4 卡 × 两个提示词 = **8 次调用**（专盯容器边界）；然后 8 张**事后新写**的挑战卡 × 两个提示词 = **16 次调用**（4 容器负例 + 4 具体核心正例，防止只对原卡过拟合）。

### 结果

| | 旧 | 候选二 |
|---|---:|---:|
| 第一轮 8 次调用 · 正确 | 1/4 | **3/4**（2 改进、**0 回归**） |
| 第二轮 16 次调用 · 正确 | 5/8 | **8/8**（3 改进、**0 回归**） |

**但第二轮的机械闸门仍判 HOLD**，因为有一道事前门要求每个正例核心同时覆盖过多语义槽位。维护者随后判断：**这道门把「核心是否正确」与「核心描述是否把细节写全」混在一起了。**

处理方式：历史机械结果**保留不改**，只标注该门过严；**不据此修改候选**。

扩大到完整 12 卡后只到 10/12，仍有两个系统性假锚点——最终仍是 HOLD。

### 否决 / 修正了什么

**① 否决「完整 12 卡只有 +1，所以前面的改进可能全是噪声」这种回读。** 两个更小的定向集上，改善方向连续、且**都没有回归**。

**② 同时否决「连续两个漂亮小集就够冻结」。** 扩大后依旧只有 10/12。**小诊断能找方向，不能替代完整门。**

**③ 再次确认：事前门也可以错。** 门错了只能保留历史判决并标注，**不能事后改门把历史分数救回来**。

### 限制

挑战集写于候选二之后，天然存在「设计者知道候选弱点」的风险；样本很小；最终未进入生产。

---

# 5. S4 · 「组件优先」的合同恢复考古

**多次真实运行 + 执行中止 + 最终完整运行。** 正文 E3 记录了最终 8/12 与「三次不许就地修补」；本节补回**合同是怎样一步步失败、又怎样与语义失败分开的**。

### 要回答的问题

「先逐条抽成分，再据成分判断有没有稳定共同核心」这个任务结构，最初反复失败。必须先分清：

> 它是真的语义上不行，还是**我们甚至还没把请求 / schema / 证据合同跑通**？

### 怎么做的

一条纪律贯穿全程：**每次只修上一轮已经明确暴露的合同问题**，不同时改提示语义、卡片或金标准；历史运行不覆盖、不重写。

### 结果：按发生顺序

| 阶段 | 实际发生了什么 | 当时可得结论 |
|---|---|---|
| 首次四卡 | 计划 96 次；实际 41 次；单条抽取 40 次全接受；**第一条聚合请求 HTTP 400** | **不可评估**，零语义结果 |
| 兼容性探针 | HTTP 成功、JSON 合法、决策方向也对，但成员列表形状不满足合同 | **中止**，仍不能评价效果 |
| 形状探针 | 成员列表与决策均正确；又被「被拒核心的区分证据」合同拒绝 | **中止**，仍属合同问题 |
| 证据合同修复 | 旧响应无需新调用即可通过；正式恢复又因被拒核心只引用一条记录而停 | **不可评估**；本阶段 4 次尝试：2 接受 / 1 拒绝 / 1 凭据致命 |
| **合同完全闭合后的四卡** | 56/56 接受；4 改善、0 回归；但四张卡在两个视图里**全部弃权** | **终于得到语义层失败** |
| 完整 12 卡 | 复用已完成部分，只跑剩余；192/192 新调用接受 | **8/12**；4 回归、2 改进；**4 张正例全部过度弃权**；候选被否 |

**只有最后一步才允许说「组件优先在该任务上失败」。** 前面任何一次 400、schema 拒绝、证据拒绝**都不能**拿来支持这个语义结论。

### 否决 / 修正了什么

**① 否决「执行失败 = 假设失败」。** 前四次里模型甚至没稳定进入可评分状态。

**② 否决「把解析器放宽一点让它过」这种恢复方式。** 恢复过程始终是把 schema / 提示词 / 解析器**对齐**，而不是为了保住输出去松闸。

**③ 合同闭合之后，才真正否决这个候选。** 它不是被接口事故淘汰的——事故修完后仍然出现非常干净的系统性过度弃权。

**④ 也否决只看负例改进。** 该候选对负例极好，甚至修掉两个长期误报；代价是**四个正例全漏**。只看假阳性会把它误判成进步。

### 限制

正式评分材料仍是合成卡；恢复过程经历多轮小探针，因此「最终合同」已吸收了前序输出形状的信息。它证明的是**在该冻结合同下这个候选失败**，不是所有 component-first 设计都失败。

---

# 6. S5 · 私有拒绝原因的零调用审计

**离线审计，provider 0 次。**

### 要回答的问题

旧实验里那些「无稳定核心 / 拒绝」的结果，**有没有足够结构化的证据**，让后续系统机械判断：模型是否识别到一个共享的具体所指？有多少成员真正支持它？拒绝一个多成员假设时，支持与反证能否被机器核对？

### 怎么做的

只审计已有的脱敏结构与必要字段，不读私有内容、不跑模型。把所需证据拆成机械可检查的条件，并把**未知数量保留为「不可得」**——禁止拿「字段看起来像有」去代替真实的正整数支持数。

### 结果

**混合 / 不足。** 具体缺口：前一阶段的冻结合同里没有可直接复用的共享所指字段；后一阶段虽有被拒的多成员假设，但「是否真是同一个具体所指」**不能由现有字段机械验证**。

### 否决 / 修正了什么

**否决了「旧输出已经含有足够证据，只差把它整理成账本」。**

> **缺的不是整理脚本，是原合同没有要求产生那种证据。**

如果后续要判断「这些记录真的指同一个具体对象」，必须在**被测合同里明确要求并单独验证**，不能从旧的锚点文本反推。

### 限制

这是字段可用性审计，不是模型能力实验。它只能说明旧证据不够支持结论，**不能说明模型识别不了共享所指**。

---

# 7. S6 · 所指账本的容量阶梯

**真实模型短冒烟 + 离线语义规则修复。**

### 要回答的问题

为「同一个具体所指」建账本后，严格 schema **连续拒绝**响应。必须先分清：

> 是语义判断错了，还是**我们给证据字段的容量根本装不下模型的正常表达**？

### 怎么做的

卡片与金标准冻结，**每次只针对上一轮已观察到的字段超限改一个容量点**；历史运行冻结，不复用旧结果冒充新 schema 结果。

### 结果

| 版本 | 停在哪 |
|---|---|
| 关键文本字段上限 64 | 两个执行臂都在第一格因证据超 64 被拒 |
| 提到 128 | 前两格接受；第三格因提及约 187 字 > 128 被拒 |
| 提及扩到 256 | 前两格接受；第三格因主张约 101 字 > 96 被拒 |
| 主张扩到 256 | 第一格又因不确定性说明约 130 字 > 128 被拒 |
| **改为只用提示词要求更简洁** | **前 3 格全部通过表示层**；前两格决策匹配，**第三格出现真正的语义不一致** |

> **直到表示层不再报错，真正的语义问题才第一次被看见。**

随后做了零调用的语义规则修复：锚点最低支持数 2 → 3；同一性证据必须指向同一个具体对象/事件/状态；共享人物、行为、容器本身不算；不同具体对象即使由同一主体执行同一行为也不得视为同一所指。新增 13 项离线规则测试全部通过——**但修复后的真实冒烟在截止线前没有执行**（见《未执行实验包补遗》）。

### 否决 / 修正了什么

**① 否决「schema 越严格越可靠」。** 硬上限小到装不下正常证据时，严格解析器只会**稳定拒绝**，不会得到更可靠的语义。

**② 否决「连续字段拒绝说明模型不会做这项任务」。** 前几轮根本没走到可评分层。

**③ 也否决「无限扩字段就能修完」。** 表示层闭合后，错误从长度超限变成明确的语义不一致——**再扩字段已经答非所问**。

**④ 一条方法纪律：表示层闭合与语义正确必须分开报。**

### 限制

短冒烟只完成极少格；每次容量调整都由上一轮失败触发，因此这是**诊断阶梯**而非无偏比较；最后的语义修复只有离线单测。

---

# 8. S7 · 首次机器样本前的合同审计

**离线工程审计，无真实数据、无模型调用。** 证据主要藏在评审评论链里，不在任何实验报告中。

### 要回答的问题

新管线「看起来能跑」时，它是否真的满足最基本的首跑合同——尤其：**模型看到的到底是不是有语义信息的、带来源的卡片；失败回退会不会悄悄丢记录；证据能不能回到真实来源；暂停后能不能正确续跑。**

### 怎么做的

不扩展后续层，只做有界评审。首轮一次性列出 **11 个主链缺口**，返修后多轮窄复验。

### 结果

首轮缺口可压成七类：

1. **判定层输入不足**：只有单元 id / 位置 / 锚点，**没有实际原文摘录、时间、谓述、成分**；
2. **失败回退会丢记录**：失败路径可能返回 0 个单元，而不是确定性保留一个；
3. **真实首跑交接未闭合**：名义上支持本地输入，实际仍只能跑合成/假 provider；
4. **关系 schema 与正本漂移**，且把「有关系」误接近「同一性合并」；
5. **证据不是来源证据**：侧别 / 来源 / 边界没有被真实来源严格核对；
6. **观察单元与语义成分混淆**：定位层被机械升级成语义层；
7. 切分后丢上下文，校验覆盖与出处过弱。

修复后给出 `READY_FOR_FIRST_MACHINE_SAMPLE`——**这句话只表示主链闭合到「可以让真实样本进入」**，不含任何效果结论。

### 否决 / 修正了什么

**① 否决「实现完成 = 可以开始读模型分数」。** 如果输入卡没有原文、回退会丢记录、证据可伪造，**后面出现任何数字都没有解释权**。

**② 否决「观察坐标可以机械升级为语义成分」。**

**③ 否决「就绪 = 实验通过」。** 它是工程前置门，不是能力门。

### 限制

代码 / 合同审计，不是语义实验；缺口数量来自一个特定实现版本。

---

# 9. S8 · fail-close 完整性离线复查

**离线复算，不新调 provider。**

### 要回答的问题

真实样本跑过之后暴露一种危险形状：**一个局部成分或依据的错误，可能把已经有效的记录框架与微单元上下文一起抹掉，最后生成空白的下游卡。**

把权威收回到来源位置、把坏成分局部丢弃之后，已有响应能否稳定重放？

### 怎么做的

依据出处改为绑定**已验证的起止位置**，并从来源记录确定性重建规范摘录；后续局部失败时**保留已验证的框架与上下文**；单个坏成分**局部丢弃并记数**；仅当内部结构全空才安全降级为整条保留。对 **20 份已保存响应**做离线重放。

### 结果

```text
响应匹配        20/20
整体失败回退    0
空白下游卡      0
无效成分        3（局部丢弃）
```

### 一处必须说明的表面矛盾

正文 E15 记录过「有 4 张卡所有结构化字段全为 `none`」。**两者不矛盾，是同一件事的前后两个时点**：

```text
E15 观察到的 4 张空白卡  →  修复前
S8 复查到的 0 张空白卡  →  修复后（局部丢弃 + 保留已验证上下文）
```

**这条差异本身就是这次修复的效果**，但它**不是 A0 语义正确率的改善**——只是不再把局部错误放大成信息黑洞。

### 否决 / 修正了什么

**① 否决「一个局部坏成分可以让整条已验证的语义上下文归零」。** 失败回退应当是**保守降级，不是制造信息黑洞**。

**② 再次否决让模型复制原文作为证据权威。** 模型给位置，系统从来源切片取权威文本。

### 限制

20 份既有响应，非随机；无新对照；「3 个坏成分」**不能外推成错误率**。

---

# 10. S9 · 关系判定两轮的后验有效性审计

**后验有效性审计，无新调用。**

> **本补遗最重要的一节。** 正文 E11 / E12 的**观测数字仍然是真的**，但其中一部分「为什么会错」的解释必须降级。

### 要回答的问题

两个此前没人检查的前提：

1. 两轮真实运行**用的到底是哪一版**「不确定」提示词？
2. 模型收到的卡片，**是否真的带了用于区分过程与事件的字段**？

### 怎么做的

只看代码、提示词版本的提交时间线、已提交报告与卡片装配路径。不读原文，不重跑。

### 结果 1：强版规则从未被这两轮测过

两轮用的是**弱版本**——只有一句「不确定时可用」。真正的强规则（证据不足时**必须**返回不确定 / 宁可不确定也不要强行确定 / 给出一个关系不要求假装有把握）**在截止线前只有零调用验收**。

> `UNCLEAR 0/4` 只能支持「**弱提示版本**在这 4 个模糊样本上没有选不确定」。
> 它**不能**支持「加强后的干预已被证伪」。

（补充：从讨论串侧独立查到，强规则后来单独测过，**仍是 0/2**——所以结论方向成立，但当时的证据不成立。）

### 结果 2：卡片装配存在系统性混杂

那两轮所处的路径里：

- 唯一被标注为锚点的字段，对整条保留单元取的是**上层项目 / 容器**；
- 上游已抽出的**记录级过程字段没有被带入**语义单元；
- 保留单元没有填时间与参与者，而当时真实样本 **0 个切分**；

因此模型看到的结构化字段系统性接近：

```text
anchor/referent: 上层容器
time: none
participants: none
```

> 模型被要求区分「同一过程的阶段」与「同一对象的不同事件」，而**最能帮它区分过程的字段被上游丢掉了，唯一明确的锚点反而是容器**。

### 结果 3：当时「不确定」在下游没有产品差异

当时实现里，不确定与无关**都不生成关系边**；预想中的「不确定值得再查」队列尚未实现。

（这一条后来被收窄：真正改变图的不是「不确定 ↔ 无关」，而是**「不确定 → 错误的肯定关系」**——后者会生成一条假边。）

### 否决 / 修正了什么

**① 把正文的旧归因从「模型倾向」降级为「输入合同诱导 + 提示词版本限制」。**

**② 否决「一个数字被归档了，它的条件也一起被归档了」。** 这一节的全部内容都来自**代码与时间线**，而不是任何实验报告——**报告只会记分数，不会记它是在哪一版提示词下测的**。

### 限制

无新调用；它不产生新的能力估计，只回答「旧分数究竟能说明什么」。

---

# 11. S10 · 线索消融：标签 / 词面 / 向量 / 话题的五种组合

**真实模型运行 + 离线复算。** 这条线在正文里完全没有出现。

### 要回答的问题

前面的实验一直在问「关系判得准不准」。这一线换了个问题：

> **到底该给模型看哪几块材料，它才可能把该在一起的放到一起？**

候选材料四块：现成标签、跨记录一字不差的词句、标签的向量、话题及其向量。

### 怎么做的

五轮，**每一轮单独当门槛测一次**——这个做法本身后来被判为错误（见下）。

### 结果

| 做法 | 结果 |
|---|---|
| **词面笨办法**（字符 n-gram + 文档频率） | 12 个锚里**只有三四个有内容**，其余全是人名 / 项目名 / 平台名 |
| **现成标签当锚** | **精度极高，但覆盖极低**：11 条只连出 **2 对**。全库 10,635 个标签里 **7,750 个只出现一次** |
| **标签向量** | **噪声压过信号**：一对无关标签的相似度 **0.537**，高于一对真正同义标签的 **0.522**。**没有阈值可选** |
| **话题 + 文体抽取**（每条 1 次调用） | 切分、文体、锚句都通过，但 **60 个话题零重复** |
| **话题向量** | 同组内部最高 **0.530** < 与组外最高 **0.692**。**当门槛不过** |

**五轮同一个形状：**

> **精确的都稀疏；能提高覆盖的，噪声都比信号多。**

### 否决 / 修正了什么

**① 否决「门槛里可以写『话题相同』」。** 60 个话题零重复意味着这样一个与门会通过 **0 对**。

**② 但这条**不能**用来否定「把话题当作参考材料之一」。** 这是本轮最重要的自我更正：

> 起初把话题单独拿去评分、判了「不过」，并写下「向量整个划掉」。
> **两条都被自己撤回**——因为**前面几步的产物是线索，不是判决**。

具体证据是两个互补的失败：

- **向量分不清「同一个人」和「同一件事」**：它把一条「某人喜欢某样东西」与一条「被同一个人嫌弃」排到第五位、相似度 0.575——**而这正是模型一眼能看出来的**；
- **模型拿不到「换了说法说同一回事」的候选**——**那是向量唯一能提供的**。

> **两边补的是对方的短板，所以必须叠着用，不能串着用。**
> **串着用，任何一环判错就整个掉；叠着用，判错只是某条线索没用上。**

**③ 由此否决了整条「先用某个线索当门槛，再交给模型」的流水线形状。** 被否的是这个形状本身，**不是某一个线索**——五个线索里没有一个够格当门槛，而它们各自漏掉的东西并不重合。

### 限制

每一轮都是小规模短冒烟；相似度数字来自极少量对照，**只用来说明「无阈值可选」，不构成分布估计**；标签统计是全库的，但「11 条只连出 2 对」的覆盖数来自一小批材料。

---

# 12. S11 · 两条横向观察

> **证据等级：维护者观察。未量化、无对照实验。**
> 记进来是因为它们改变了后续实验该怎么设计，而不是因为它们有数据。

### 观察一：两两对比这条路，时间和费用都撑不住

前面绝大多数实验的基本动作都是**成对判断**——给模型两个单元，问它们是什么关系。

实际执行下来：**耗时超长、费用也高**，而且**代价随材料量呈平方增长**。

这一点与已经量到的东西是一致的：

- 单次运行的分数没有分辨力（正文 E14：联合稳定 **0/10**），所以**每个结论都需要重复采样**——成对判断的次数再乘以重复次数；
- 而重复采样又发现整轮之间存在相关，**有效样本量还小于观测数**。

> **成对判断 × 重复采样 × 平方增长**，三者叠起来，这条路在真实规模上跑不起来。

它指向的替代形状与 S10 的结论同向：**先用多种线索一起圈出宽松的候选组，把整组一次交给模型判断**，而不是把组内所有对都问一遍。

**这条从未被正式对照过**（成对 vs 整组的成本与质量对比不在任何一轮实验里）。

### 观察二：感想类文本上，精确字段可能强过话题向量

在**感情 / 感想类**材料上，维护者观察到：

> **用话题向量之类的做法，准确率甚至可能不如直接按相同字段搜索、直接合。**

这与 S10 的两个数据点方向一致——现成标签**精度极高**（只是覆盖低），而话题向量**当门槛不过**（组内最高低于组外最高）。

**为什么值得单独记**：它指出**方法的优劣可能随材料体裁翻转**。

- 事件类材料有具体锚点（时间、对象、推进），语义方法有东西可抓；
- **感想类材料的「同一件事」往往就是同一个词**——共享的是措辞与关注对象，而不是一个可指认的事件。此时精确匹配抓到的正是那个共享物，而语义相似度反而会把「同一个人的不同感受」拉到一起。

这与正文 E16 的一条限定互相印证：那批第一屏材料**偏反思、几乎没有事件可串**，因而「基于字面共现的笨办法占便宜，基于关系判断的方法吃亏」。

> **两处独立出现的同一现象，值得当成一个待测假设，而不是一次偶然。**

### 未做的最小验证

两条都没有被量过。可执行的最小对照各一条：

```text
观察一：同一批材料，成对判断 vs 整组判断
        比调用次数、总耗时、以及同一批金标准上的结论差异

观察二：把材料按体裁分成「事件类」与「感想类」
        在两组上分别比较「精确字段匹配」与「话题/向量」的表现
        看优劣是否真的翻转
```

第二条尤其便宜——**不需要新调用，用已有的标签与向量重算即可**。

---

## 13. 这份补遗最该被记住的一条

十一项补遗里有五项（S4 / S5 / S6 / S7 / S9）说的是同一件事的不同侧面：

> **在读一个语义分数之前，先确认那个分数所依赖的合同真的闭合了。**

- 合同没闭合时的失败**不是**语义失败（S4）；
- 字段装不下时的拒绝**不是**判断错误（S6）；
- 旧证据不足**不等于**模型不会（S5）；
- 「能跑」**不等于**输入卡里有语义信息（S7）；
- **分数被归档了，它的条件不会被归档**（S9）。

而 S10 / S11 指向的是另一件事：

> **在问「模型判得准不准」之前，先问「我们到底该给它看什么，以及该问它多少次」。**
