# 个人记忆聚合：实验记录

> 性质：**过程记录与否决理由**，不是方案推荐。本文中没有任何一条结论达到「可以照搬」的强度。  
> 组织方式：**一个测试一节**，每节按同一模板写。  
> 脱敏：人名、项目名、产品名、文件路径、分支/工单编号、日期、私有产物位置均已移除。第三方论文名、公开系统名、模型与厂商名予以保留以便复核。

---

## 0.0 本版更正了上一版的四处结论

上一版只读了仓库文档与分支；本版补进了设计复核讨论串。**讨论串里的记录直接推翻或收窄了四条此前写成结论的东西**，列在最前面，因为它们改变的是别的章节该怎么读。

| # | 上一版怎么写的 | 更正 |
|---|---|---|
| **1** | 把「弱前缀正则会命中『见 2026』『同 200 元』」当作 5 条断链的成因 | **假设已被证伪。** 修好正则后在同一份快照上重跑：`7 命中 / 2 可解析 / 5 断链`，**一个都没变**——那几路弱前缀在真实库里一次都没命中过。5 条断链全部是 16 进制、像记录标识的取值。覆盖率**就是** 0.4%，不是「可能更低」。正则教训本身仍成立（它是一类真实风险），但**它不是这批数据的成因**。详见 E6 |
| **2** | 「模型系统性回避不确定，可复现（0/4）」 | **结论方向仍成立，但范围写错了。** 那 0/4 是在一个只有**半句**「不确定时用 UNCLEAR」的提示词版本下测得的；真正的强规则（「证据不足时**必须**返回不确定」）在当时**从未被 provider 跑过**。后来单独测了强规则版本，两个刻意做成模糊的案例**仍然是 0/2**。所以正确表述是：**强弱两版提示词都试过，都没能让模型在模糊案例上选不确定** |
| **3** | 关系判定「原始 7/10、校验后 6/10」等分数 | **这些分数没有分辨力。** 同一合同下重复采样显示：联合 `(锚点, 关系)` 完全稳定的案例是 **0/10**，三次重复的关系命中是 6/10、5/10、5/10。**±1 与噪声不可分。** 前面 E11 / E12 的所有数字都应读作**单次抽样**，不是能力估计。详见 E14 |
| **4** | 「同一过程的不同阶段 vs 同一对象不同事件是主要语义混淆点」 | **现象成立，归因需要改。** 送进模型的卡片里：唯一被标注为锚点的字段取的是**项目容器名**，记录级的过程字段在物化时**被丢弃**，时间与参与者两栏 **20/20 恒为 `none`**；而且六个关系标签里**只有「同一件事」在提示词中被定义过**。详见 E15 |

**一条通用的读法**：上一版是从**已归档的文档**整理的，而文档只保留收口后的结论。**过程、撤回、以及「这个数是在什么条件下测的」大量留在讨论串里**——这四条更正全部来自那里。

> **与补遗 S9 的关系**：第 2 条与第 4 条，补遗 `EXPERIMENT_SUPPLEMENT.md` 的 **S9** 从**代码与提示词版本时间线**独立查到了同一结论；这里是从**讨论串**查到的。两处互为佐证，**读一处即可**。第 1 条与第 3 条是本版独有。

---

## 0. 这份文档是什么

一个单人书写的中文个人记忆库（约 1,600 条）上，为「怎么把记忆切成合适的单元、怎么把散在不同记录里的同一件事认回来、什么时候一组东西才值得成为一个篮子」做的一系列测试。

**总共十六项**：四项零成本统计 / 合成校准，三项人工标注，九项真实模型运行。

**每一项都按下面五格记：**

```
要回答什么问题  →  怎么做的  →  结果是什么  →  否决了什么  →  这个结论有多弱
```

**没有一项证明某个聚合方案可用。** 每一项的产出都是**删掉一个此前被默认为真的前提**。这份文档的价值在删除，不在建设。

阅读时请注意第五格。除两项全库统计外，样本量都在个位数到六十之间，绝大多数结论只有**存在性**含义，没有比例含义。

---

## 1. 实验对象

### 1.1 语料形状

单人书写的中文个人记忆库，只读快照 **1,596 条**。

它有几个对方法选择影响很大的特点，值得先说清楚，因为它们与大多数公开评测的语料**恰好相反**：

| | 主流评测语料（新闻 / 文档 RAG / 多方对话） | 本语料 |
|---|---|---|
| 作者 | 多方独立记述 | **单一作者** |
| 事实锚点 | 密集（时间、地点、人名、数字、机构、版本号） | **稀疏** |
| 共同背景 | 大多写在文本里 | **大量位于文本之外** |
| 长度 | 段落到长文 | **中位 34 字** |
| 是否已被切好 | 否 | **大部分已经是一条一事** |

**后果**：任何依赖「同一事件被多方独立记述、且记述中含可对齐锚点」的方法，在这里的前提不成立。这不是说那些方法差，而是它们的**适用条件在这里不满足**。

### 1.2 三个必须分开的概念

很多关于「切分该多细」的争论，其实是把三件事混在一起：

| 概念 | 它回答什么 |
|---|---|
| **语义单元** | 这段材料在人类语义上是不是一个局部完整的经历 / 过程 / 状态？ |
| **检索单元** | 建索引时用多大的片段最容易命中查询？ |
| **检索表示** | 同一个检索单元用什么表示进索引？ |

绝大多数切分文献做的是后两件。本组测试做的是第一件。

这个区分不是文字游戏——它决定了一条重要的负面证据（语义切分的额外成本换不来稳定的端到端收益，见 §19.5）**能否用来反驳按语义找人可读单元这件事**。答案是不能，因为那些评测针对的是检索单元。但这**只解除了一半**，另一半仍然空着——**「按语义找出的单元对读的人是否更有用」从来没有被测过**。

---

## 2. 实验总表

按发生顺序。**不给日期，只给顺序**——各项之间的间隔、迭代次数与工期不在本文范围内。

| # | 名称 | 类型 | 规模 | 一句话结果 |
|---|---|---|---|---|
| **E1** | 向量密度扫参 | 机器 | 36 组 HDBSCAN | 无密度簇 |
| **E2** | 真实卡组的直接硬判断 | 真实模型 | 12 组 × 5 条 = 60 张真实卡 | 反向对齐、关系容器、宽主题四舍五入 |
| **E3** | 合成卡组的「容器 vs 核心」候选选拔 | 真实模型 | 12 组 × 5 条 = 60 条合成记录，4 个提示候选 | 最好 10/12；新候选系统性过度弃权被否 |
| **E4** | 切分标注格式与一致性管道的合成校准 | 合成 | 30 例（10 类 × 3） | 只验证管道，不验证语义 |
| **E5** | 语料长度普查 | 零成本统计 | 1,596 条 | 中位 34 字；一成记录装一半以上文字 |
| **E6** | 显式跨记录引用扫描 | 零成本统计 | 1,596 条 | 覆盖率 0.4% |
| **E7** | 切分一致性 · 两人对照 | 人工标注 | 3 条，2 人 | 分歧是判准不是粒度 |
| **E8** | 跨记录同一性 · 手工版 | 人工标注 | 6 对，2 人 | 瓶颈是召回不是判断；三项结论撤回 |
| **E9** | 切分一致性 · 三人独立 | 人工标注 | 9 条，3 人 | 分歧多为嵌套；排版不是边界 |
| **E10** | 切分门的首次真实样本 | 真实模型 | 12 + 8 条 | 强保留偏置，0 次切分 |
| **E11** | 关系判定第一轮 | 真实模型 | 10 对锁定金标准 | 校验后 5/10 |
| **E12** | 关系判定第二轮 | 真实模型 | 10 对锁定金标准 | 校验后 6/10 |
| **E13** | 三项契约修复 | 离线 | 无模型调用 | 证据契约、不确定性、可恢复运行 |
| **E14** | 噪声底：同一合同重复采样 | 真实模型 | 10 对 × 4 观测 | 联合稳定 0/10；单次分数不可归因 |
| **E15** | 卡片修复的同期配对对照 | 真实模型 | 60 次调用 | MIXED；对照组被自己的实现毁掉 |
| **E16** | 第一屏：人机并排 + 选择性追溯 | 真实模型 + 人工 | 12 条真实记录 | 价值混合；选择性追溯不成立；弱边与集合可分不可兼得 |

**贯穿全部人工/模型测试的三条纪律**：

1. **人工金标准在看到模型输出之前封存**；
2. **候选不得由被测系统自己生成**（否则是循环证明）；
3. **语义分歧与证据/契约拒绝分开报**，绝不合成一个数。

---

# 3. E1 · 向量密度扫参

### 要回答的问题

这个库的向量空间里，是否存在可被密度方法发现的簇结构？——即「embedding → 聚类 → 生成簇摘要 → 摘要再 embedding → 继续递归」这条常见路线的前提是否成立。

### 怎么做的

在真实语料上做 **36 组 HDBSCAN 扫参**，观察轮廓系数（silhouette）。

### 结果

**silhouette 落在 0.155–0.165。**

### 否决了什么

这个区间的含义不是「参数没调好」，而是：**该语料的向量空间里不存在可被密度方法发现的簇结构**。继续调参不会改变结论。

由此否决的不是某个具体算法，而是一整类做法的**语义裁决权**：

> 「相似 → 成簇 → 摘要 → 再聚类」这条递归，作为多层检索与可视化骨架仍然有用；但它不能用来宣布「这些东西共同属于一个主题」。

具体理由有三条，其中后两条与本语料无关，是这类方案的通病：

1. 底层距离接近**不等于**小簇具有共同事件或过程；
2. 每个聚类结果都生成摘要，会把**偶然组合固化为父主题**——大簇材料多，通常真能说出一个宽主题；小簇只有两三条，最容易出现「听起来正确、但其实非常空」的标题；
3. 同一个递归算子同时负责「小事件簇」和「高层主题」，**混淆了相似与涵摄**。

### 限制

单一语料、单一聚类族。这不能证明向量方法在别的个人记忆库上也无效，只能证明**在这一个库上，密度不是可用信号**。

### 附带发现：旧原型自身的五个缺陷

复查旧原型时发现五处与算法无关的缺陷，第一处特别值得记录：

| # | 缺陷 | 后果 |
|---|---|---|
| **B1** | 验收闸门**算术上不可能通过**：拿 `A/(A+B)` 与 `max(A,B)/(A+B)` 比，最好只能相等 | 不管数据说什么，都会读到「贴着基线」 |
| B2 | 缓存写入用未 strip 的文本算哈希，读取用 strip 后算 → **缓存键永不命中** | 静默全量重跑，只表现为「怎么这么慢」 |
| B3 | 脱敏开关没盖住聚类解释词和模型命名字段 | 脱敏不完全 |
| B4 | 注释里第一条触发条件根本没实现 | 文档与代码不符 |
| B5 | 死代码若干 | 清扫级 |

**B1 是后面反复出现的一类错误的原型**：一个看起来严谨、实际上**分不出「做对了」和「它总是这么答」**的指标。E8、E9 各自又撞到了它的变体。

---

# 4. E2 · 真实卡组的直接硬判断

**这是第一次让模型在真实材料上做聚合判断。**

### 要回答的问题

把前一步聚类产生的候选簇原样交给模型，让它**直接硬判断**：

- 这一簇里每个成员是**核心 / 边缘 / 应该剔除**？
- 这一簇整体该**保留 / 收缩 / 拆分 / 打散**？
- 它们共同的具体主张是什么？

一句话：**模型能不能替人判断「这一组值得在一起吗」。**

### 怎么做的

- **材料**：12 个候选簇，每簇 **5 条真实记录**——共 **60 张真实卡**。簇本身由上一步的聚类产生，因此其中天然包含宽主题簇。
- **任务**：一次调用同时给出三样东西——成员角色（`core / weak_boundary / remove`）、整簇处置（`RETAIN / SHRINK / SPLIT / RESIDUAL`）、共同主张（一句话）。
- **参考答案**：人工逐条给出角色与处置，用于对照。
- **硬门**：在模型输出上跑几条可机械判定的闸门，例如「主题是否太宽」「替换测试是否失败」「是否与参考冲突」。任一硬门失败 → 该簇交人工复核，不计作通过。
- **提示词迭代**：一共试了 **9 个版本**，最终选中其中一版。前一版与选中版之间做了逐位置的结构化 diff。
- **成本**：全部版本合计二十余次调用，总花费不到 1 元人民币（预算上限 2 元）。

### 结果

**逐簇结论：12 簇中 5 簇通过，7 簇需人工复核，其中 2 簇硬门直接失败。**

**版本迭代的收益极小。** 选中版相对前一版，在两版都跑过的三个簇里**只有 2 处角色翻转**，且都发生在同一个簇上——方向对（剔除 → 边缘），但**没有走到参考答案要求的核心**。

**选中版对参考答案的 21 处不一致，形状高度集中：**

| 不一致类型 | 数量 |
|---|---:|
| **过度提升**（模型判核心，参考判边缘） | **10** |
| 提升不足（模型判边缘，参考判核心） | 2 |
| **误杀**（模型判剔除，参考判核心或边缘） | 5 |
| **误留**（模型判核心，参考判剔除） | 4 |
| 整簇被剔除 | 0 |

**三个最严重的失败，每个代表一类：**

**① 反向对齐。** 某簇里模型认定的核心是第 4、5 条，而参考答案的核心是第 1、2 条——**两者几乎互为反面**。模型抓住了一个更窄的表层主题，把真正的核心成员全部剔除。

**② 关系容器被当成共同核心。** 某簇的参考答案是「无稳定核心」，但模型把 3/5 判为核心，理由是它们涉及同一段关系。**模型无法区分「一段关系」与「一个具体的共同物」。** 这一簇被标为「已证明不适用」。

**③ 宽主题四舍五入。** 某簇模型把 4/5 判为核心，共同主张是一个很宽的情绪类主题——把不同内容类型的材料圈进了同一个筐。硬门「主题太宽」与「替换测试失败」同时触发。

**一个贯穿全局的偏置：模型系统性地把边缘提升为核心。** 21 处不一致里 10 处是这个方向。

### 否决了什么

**① 否决了「继续调提示词能修好宽主题簇」。**

迭代到第 9 版时，新加的反例只修好了它针对的那一类（命名实体密集的核心、命名空间混合需要拆分），**对关系容器与宽主题一点用都没有**。

结论写得很直接：

> **核心 ↔ 边缘 才是主要结构问题，不是 核心 ↔ 剔除。**
> **应当退回聚类层去减少宽主题簇，而不是继续在判断层加反例。**

这条是整条线上最有价值的一次「往回退」——它把力气从「让模型判得更准」转向「别给模型送一个本来就不成立的组」。

**② 否决了「让同一次调用同时给角色、处置和主张」这种打包任务。** 某簇出现了自相矛盾的输出：处置是「收缩」，却把 4/5 都判成核心——收缩要求剔除成员，不是留下。打包任务让这类矛盾不容易被约束住。

**③ 否决了把这一版直接推到全库。** 明确记为「有条件」，条件就是先解决宽主题簇。

### 限制

12 簇、60 张卡；单一模型；参考答案由参与设计的人给出；簇来自一个已知有缺陷的聚类步骤，因此**部分失败可能是上游造成的，不能全部记在判断层头上**——这也正是「退回聚类层」这条建议的由来。

---

# 5. E3 · 合成卡组的「容器 vs 核心」候选选拔

### 要回答的问题

E2 暴露的核心问题是**容器（同一个人、同一段关系、同一个任务、同一个宽主题）被当成共同核心**。

于是把问题收窄成一个**二选一的硬判断**：

> 给定一组记录，它们**是否存在一个稳定的共同核心**？
> 输出只有两种：`ANCHOR`（有）或 `NO_STABLE_CORE`（没有）。

并在这个收窄的问题上**做候选提示词的正式选拔**。

### 怎么做的

**为什么改用合成材料**：真实卡片不适合反复用于提示词选拔（会过拟合到具体记忆），也无法预置需要的陷阱。因此整套材料改为**新写的、中性的虚构内容**，不含真实记忆、真实人物或真实项目。

**材料**：12 组 × 5 条 = **60 条合成记录**。金标准分布：`ANCHOR` × 4，`NO_STABLE_CORE` × 8。

**场景是按陷阱类型设计的**，不是随机生成：

| 场景 | 金标准 | 它测什么 |
|---|---|---|
| 明确有核心 | ANCHOR | 基本能力 |
| 明确无核心 | NO_STABLE_CORE | 基本能力 |
| 5 条里只有 2 条共享 | NO_STABLE_CORE | 支持度门槛 |
| 5 条里有 3 条共享 | ANCHOR | 支持度门槛 |
| **同一批人，但各做各的事** | NO_STABLE_CORE | **人物容器诱饵** |
| **同一段关系** | NO_STABLE_CORE | **关系容器诱饵** |
| **同一种行为模式** | NO_STABLE_CORE | **模式容器诱饵** |
| **同一个持续变化** | NO_STABLE_CORE | **变化容器诱饵** |
| 很窄的反向对齐 | ANCHOR | E2 那个反向对齐失败 |
| 一个核心两个边界 | ANCHOR | 边界精度 |
| 多个小核心并存 | NO_STABLE_CORE | 是否会强行合成一个 |

**每个金标准还带三类标记**：可接受的核心表述、**宽主题标记**、**关系容器标记**。判「宽泛 / 容器 / 过窄」时依据这些预先写好的标记，**不由测试装置自己猜语义**。

**实验纪律（这一节比结果更值得看）：**

1. **卡片与答案分两个文件保存**，并由一条测试断言「卡片文件里不出现答案字段」；
2. **通过门在第一次调用之前提交**，看到输出后不得修改；
3. **确定性交叉平衡的调用顺序**：用固定种子打乱卡序，两个候选交替，第二轮反向再交错，顺序写死在文件里，执行时不得重排；
4. 温度 0、关闭思考、固定最大输出、**重试 0**（未知错误一律按致命处理）、成本上限；
5. 每卡跑两种视图（原始 / 已切分），且**两种视图共用同一金标准**——因为金标准由「有多少条记录支持」决定，而切分只在记录内部发生，不改变记录条数；
6. **两条退化策略被专门的测试堵死**：
   - 「全部弃权」不得被记成成功（弃权召回率会是 1.0，但过度弃权数等于全部正例卡，决策正确率下降）；
   - 「全部判有核心」会复现旧提示词的地板效应。

**第 6 条不是假想**：**旧提示词的实际表现就是从不弃权**。它把弃权在语用上写成了「任务没完成」，于是模型总是给一个核心。这正是要修的东西。

**被测候选**：旧提示词（基线）、候选一、候选二、以及一个改变任务结构的「组件优先」候选（先逐条抽成分，再聚合成分）。

### 结果

**基线与候选二的正式对比**：基线 **9/12**，候选二 **10/12**，1 处改进、**0 处回归、0 次过度弃权**。候选二因此成为保留基线，但**没有被选进生产**。

候选一未通过语义冻结。

**组件优先候选：被否决。**

| 门 | 结果 |
|---|---|
| 正确率 ≥ 11/12 | **FAIL：8/12** |
| 回归 = 0 | **FAIL：4 处** |
| 过度弃权 = 0 | **FAIL：4 卡 / 8 个视图行** |
| 指定的两张难卡至少改进 1 张 | PASS：两张都改进 |
| 无新的系统性失败 | **FAIL：4 张正例卡全部漏判** |
| 两次重复一致 | PASS |

**它的失败形状非常干净**：8 张负例卡**全对**，还修好了此前两张一直误报的；但**4 张正例卡全部输出「无稳定核心」**，在两种视图、两次重复下都一样。

这是**系统性的过度弃权 / 漏锚点**，不是零星的标记不匹配。

一个重要的细节：**这个候选把「宽泛地判有核心」换成了「一律弃权」，而弃权在负例上看起来像是改进。** 如果只看「误报下降了」，会误判成成功——这正是第 6 条测试要堵的那条路。

### 三次「不许就地修补」

这一轮里有三处，按纪律**没有当场修补**：

**① 一次运行在第一个聚合请求上就 HTTP 400 致命失败。** 此前 40 次单条抽取全部被接受，聚合请求 0 次被接受。处理方式：

- 记为 `ABORTED`，效果判定记为 **`INCONCLUSIVE`**——「既不支持也不反驳该假设」；
- 改进数与回归数记为 **`NOT_EVALUABLE`，而不是测得的 0**；
- **不允许**紧接着改提示词、改 schema、加字段、换模型或改卡片；
- 后续阶段不获授权。

**② 一次早期的四卡冒烟给出的闸门拒绝，事后被判定为「对全负例集合无效的规则输出」。** 处理方式是**保留原记录不改写**，另行标注它无效——而不是回去把历史结果改对。

**③ 一次事后追加的挑战集里，有一道门要求「每个正例的核心必须同时包含实体、对象、事件、时间」。** 维护者裁定**这道门对这一层过严**，因此**不得据此修改候选**。

### 后来对「为什么会这样」的一次分析

当时这个问题被明确记为「未来研究问题」，没有当场给解释。后来在一次只读金标准的分析里得到了一个**更好的解释**，值得写在这里——它改变了这一项该被怎么读。

把 4 张漏判的正例与 2 张被正确拦下的硬负例并排：

| | 支持记录数 | **一致的是什么** | **不一致的是什么** | 金标准 |
|---|---|---|---|---|
| 正例 ×4 | **3/5** | 具体对象／事件（某处菜园的那次秋收、某家店的营业时间变更、某站的信号故障、某站点的扩建） | 表述细节（"新增车架" vs "额外车架"） | 有核心 |
| 硬负例 ×2 | **3 条** | 主体 + 行为模式 / 主体 + 持续变化 | **对象**（三份不同名单）／**领域**（三个不同方向的进步） | 无核心 |

> **两类卡在「重合了多少」这个维度上完全重合**：真锚点全是 3/5，硬负例也全是 3 条。
> **唯一的区别是：一致发生在「对象」上，还是发生在「容器」上。**

这就解释了这一项的失败形状。「组件优先」把记录拆成成分之后，问的是**成分重合得够不够多**；而两类卡都只有部分重合——负例重合了主体和行为、差在对象，正例重合了对象、差在细节。

> **用「重合度」当判据，这两类不可分。拆得越细，越容易找到差异，于是正例和负例一起被拒。**
> 所以真正的成因**不是「成分拆得太细」，也不是「锚点定义太严」——是判据测错了量**。

同一批卡还能用谓词—论元结构复述一遍，8 张判别卡**全中**：受事／主题相同 → 有核心；施事或谓词相同、受事不同 → 无核心。

**这也给这类任务定了位**：它的学名是**跨文档共指消解**，而该领域一开始就命名了这里观测到的两种失败——**同一指称、不同描述 → 漏判**（4 张正例）；**不同指称、相同描述 → 误判**（此前一直误报的那两张）。「组件优先」只按住了其中一边。

**这条分析的证据等级要说清楚**：它来自**金标准的标注理由**，不是模型的实际输出。后来专门做了一次离线审计去核它（见补遗 S5），结果是**核不了**——归档产物里没有「是否识别到共享所指」这个字段。

> 所以「判据维度坍缩」是目前**最好的解释**，**不是被验证过的结论**。

### 否决了什么

**① 否决了「组件优先」这条任务结构。** 先逐条抽成分再聚合，在这个任务上产生了系统性的正例压制。当时未给解释；后来的分析（见上）指向判据维度坍缩，**但该解释至今没有运行证据支持**。

**② 否决了「弃权率下降 = 改进」这种读法。**

**③ 否决了「看到输出之后再定门」。** 全部门在第一次调用前冻结；事后觉得某道门不合适，只能**记录它无效**，不能回改。

**④ 否决了把任何候选选进生产。** 最好的候选只有 10/12，且这一轮结束时明确写下：不选生产候选、不设计下一版、该实验链关闭待复核。

### 限制

全部材料是**合成的**——它验证的是「模型能否抵抗预设的容器诱饵」，**不能证明在真实记忆上也如此**；12 卡的样本量对 11/12 这种门槛而言非常脆（一张卡就是 8 个百分点）；提示词与场景由同一批人设计，存在设计者偏置。

---

# 6. E4 · 切分标注格式与一致性管道的合成校准

### 要回答的问题

在把人拉进来做切分标注之前，先确认：**标注格式和一致性计算的管道本身是通的吗？**

### 怎么做的

- **30 例合成校准材料**（10 类 × 3）；
- 两名「标注者」是**合成夹具**，不是真人；
- 跑两个切分器：`S0`（结构基线）与 `S1`；
- 计算：决策一致率、有害假切分、漏切、归属跨度 IoU、上下文精确率/召回率、替代方案保留数、以及若干泄漏检查（事件源、写入时间污染、标注原文泄漏）。

### 结果

**报告开篇就写明了 non-claims**——这一点比数字本身更重要：

> - 本报告验证的是标注**格式**与一致性**管道**；
> - A/B 是**合成夹具，不是真实标注者数据**；不报告任何人类上限或真实一致率；
> - 真实私有样本**尚未开始**。

管道数字（合成夹具之间）：决策一致 30/30，跨度 IoU 1.0，上下文重合 1.0。**这些数字只说明管道通了。**

两个切分器的形状：

| | `S0` | `S1` |
|---|---|---|
| 漏切（难例） | **12/30** | 0 |
| 把明确该切判成「不确定」 | 0 | **8** |
| 「不确定」的精确率 / 召回率 | 0 / 0 | 0.43 / 1.0 |
| 生成替代切分方案 | 0/3 | 0/3 |

`S0` 把**所有** 30 例都判成「不切」——包括 12 例明确该切的。`S1` 不再漏切，但把 8 例明确该切的降级成「不确定」，即用**升级不确定**换掉了漏切。

替代方案一项，报告明确记为**能力缺口，而不是失败**——因为 `S1` 本来就不具备生成替代方案的能力。

### 否决了什么

**它没有否决任何语义结论——因为它压根不产生语义结论。**

它真正的价值是一个**反面示范的反面**：一次完全可以被误读成「切分器工作良好」的运行，报告自己在开头把误读堵死了。

**可复用的一条**：**在管道验证报告里显式写出 non-claims**，比在结尾加免责声明有效得多。

### 限制

全部是合成夹具；所谓「一致率 1.0」不含任何真实标注者信息，**不得作为一致性证据引用**。

---

# 7. E5 · 语料长度普查

### 要回答的问题

两个都不能靠猜的数：

1. 这个库的文字量堆在哪里？
2. 「短到不必检查该不该切」这条优化，能覆盖多少？

### 怎么做的

纯本地统计，不调模型、不联网、不写任何东西。对每条记录取归一化后的字符数，出分位数；再按几个候选门槛算「低于它的记录占比」与「这些记录里提示词固定开销占多大比例」。

（脚本见 `reference-code/length_census.py`。）

### 结果

```text
p50  34 字    p75  53 字    p90  183 字
p99  692 字   最长 1,581 字
62.7% 在 40 字以内     77% 在 60 字以内
```

**文字量分布**：200 字以上只占约 **10%** 的记录，却装着**一半以上**的文字。

### 否决了什么

**① 否决了一份成本估算。** 早期成本表按平均 600 / 1000 / 1800 input token 估算，得出「全库跑一遍约 15–20 元人民币」。实际 p50 约 24 token，同一公式下是**三五元**——差一个量级。

**② 否决了一个自己刚下的结论。** 最初只按条数说「切分基本不是问题」。按文字量算完全不是——**切分针对的正是那一成长记录**。

> 这是一次口径错误：选了让结论更干净的那个计量口径。**同一份数据换个口径就翻转的结论，要两个口径都报。**

**③ 收窄了长度这个信号的用途。** 后来有了少量人工标注后测得：**不需要切的记录中，有若干条比「最短的、确实需要切的那条」还长**。

因此长度与该不该切**没有干净的分界**。它最多只能用于证明「这一条无需检查」，**绝不能用于「它很长，所以应该切」**。

### 限制

字/token 换算用的是中文粗估默认值，只给数量级；正式使用前应当拿一次真实调用的 usage 字段校准。

---

# 8. E6 · 显式跨记录引用扫描

### 要回答的问题

语料正文里存在作者手写的记录间引用（形如「接 <记录标识>」）。这条通道性质很好：免费、高精度、**不依赖词面重合**（因此能跨过改名）。

**它能覆盖多少？** 够不够当候选召回的主干？

### 怎么做的

正则扫全库，统计三个数：覆盖率 / 库内可解析 / 指向不存在的标识。不调模型、不算向量、只读只统计。（脚本见 `reference-code/scan_explicit_refs.py`。）

### 结果

| 项 | 值 |
|---|---|
| 记录总数 | 1,596 |
| 带疑似显式引用的记录 | 7（**0.4%**） |
| 引用对 | 7 |
| 库内可解析 | 2（28.6%） |
| 指向不存在标识 | 5 |
| 参与引用的记录 | 4 |

### 否决了什么

**否决了「手写显式引用可以当召回主干」。** 0.4% 的覆盖率只够当一条零成本的加分通道。

同时**修正了一条更早的硬结论**：此前记的是「这个库里没有显式关系」。准确表述是「**结构化关系字段为空**」——关系本身存在，写在正文里，只是从未被抽取。两者都成立，但说的不是一回事。

### 一个正则教训（值得单独记）

第一版把「见 / 同 / 续 / 承」这些**单字**前缀也配上纯数字 ID，于是「见 2026」「同 200 元」「续 3 天」全部命中——在一个满是日期和数量的语料里，**那就是个假阳性发生器**。5/7 dangling 很可能就是这么来的。

修正为两等前缀：

```text
强前缀（明确表示接续）      → 纯数字 ID 也认
弱前缀（日常用词，容易撞）  → 只认 16 进制形态的 ID
```

并且**把命中标识的「形状」单独打出来**（像 ID 的 / 像年份的 / 两三位裸数字），一眼就能看出假阳性集中在哪。

> 更一般的教训：**一个召回通道的覆盖率数字，必须连同它的假阳性形状一起读。** 只报覆盖率会把正则缺陷读成语料性质。

### 限制

只见过一种引用写法，正则表可能不全；「指向不存在的标识」有两种解释（历史标识失效 / 正则假阳性），本轮未完全区分。

---

# 9. E7 · 切分一致性：两人对照

**规模：3 条真实记录，2 名标注者。不具统计意义，只当信号读。**

### 要回答的问题

在没有金标准的情况下，评测不该问「模型判得对不对」，该问**标注者之间一不一致**。这是那个测试的最小版本。

### 怎么做的

1. 挑三条覆盖不同形态的记录：
   - **记录 A**：短，多件事，全靠句号分隔；
   - **记录 B**：规则类，有空行分块；
   - **记录 C**：标题自称「合并桶」，正文由多个方括号小节组成。
2. 一方**先切完并封存**，附**事前写下的预测**，不给另一方看；
3. 另一方独立切；
4. 对照。

第 2 步是刻意的：**金标准若照着被测规则写，分数就没有信息。**

### 结果

| | 标注者一 | 标注者二 | |
|---|---|---|---|
| 记录 A | 2 段 | 3 段 | **分歧一处** |
| 记录 B | 不切 | 不切 | 一致 |
| 记录 C | 4 段，边界在各小节标题处 | 同左 | 完全一致（含「不再往下细分」） |

**边界一致率 4/5（并集）。**

封存的四条预测，**三条错**：

| 预测 | 结果 |
|---|---|
| 记录 A 的分歧会落在某个特定切点 | **错**，分歧在另一处，两人都没在预测处切 |
| 记录 B 可能被按空行切成三块 | **错**。只有旧代码会被空行误导，人不会 |
| 记录 C 的分歧会是「要不要再细分一层」 | **错**，两人都没细分 |
| 分歧主要由粒度决定 | 半对 |

系统性偏差：**把分歧预测在了错的位置，并且高估了分歧数量。**

### 最重要的发现：分歧不是粒度，是判准

唯一那处分歧上，两人用的是**不同的切分依据**——一方按「一段连续的相处」切，依据是**事件的连续**；另一方用的是另一种表层判据。

而且同一位标注者给另一段写下的**不切**理由是「因为 X 之前就在 Y 上」——那是**因果／位置**关系。

> **人实际写下的连贯依据，是事件的连续、因果与具体时空关系。**
> **表层判据虽然容易实现，但不是人做这个判断时用的东西。**

### 否决了什么

**否决了「底层的『合』会被『切』消化掉」这个前提**——即「只要切得干净，同类的段自然就落在一起，不需要单独的聚合动作」。

改为：

```text
「切」生成更干净的叶子；
「合」是跨叶子识别同一事件／过程。
切显著降低合的难度，但不消灭它。
```

这个改动在记录 A 上直接显形：那处被一方切开的内容，是同一次经历**内部**发生的事——按表层判据切会把一次完整经历切碎。

### 两项附带发现

**① 一条记录同时记着规则和对该规则的违反。** 记录 C 记载的第一条规则要求「每条只写一件事」，而它自己的标题写明是多件事合并而成的桶。此前对「部分多事件记录来自写入时自动合并」只是推测，**这是明写的证据**。

**② 出现了一次跨记录的同一事件。** 记录 A 的末段与记录 C 的末节记的是同一件事，落在两条不同记录里，且两人都独立地把它划为独立一段。

**但它是弱阳性**：n=1、事后发现、两侧共享专名。因此它的解释必须不对称：

> **失败说明链路存在硬伤；成功只说明最容易的一类链路没有断，不能证明方案成立。**

### 限制

n=3；两名标注者；材料由其中一方挑选。样本太小，不足以支撑任何比例性结论。

---

# 10. E8 · 跨记录同一性判定：手工版

**规模：6 对片段，2 名标注者，其中一名是出题者——不独立，不是统计。**

### 要回答的问题

「把散在不同记录里的同一件事认回来」这条链从未验过。手工测三件事：**判得出来吗 / 靠什么判的 / 找得到吗。**

### 怎么做的

1. 从语料中挑六对片段，覆盖多种关系类型，**不标难易、不标预期答案**；
2. 出题方**先完成判断并连同预测一起封存**，标注者未见；
3. 标注者独立作答；
4. 另有三项：遮名复判、线索来源勾选、召回小测；
5. 对照。

### 结果

配对判断 **6 对中 5 对一致**。

**但这个一致率强度很低**（两名标注者不独立，材料由出题者挑选），因此**不作为结论使用**。

本轮真正的产出是下面三项撤回。

### 三项撤回（本轮最有价值的部分）

**① 撤回「遮名后失效 3/6」。**

该比率不可解释。分子混入三种不同状态：

- 确定失效；
- 变得不确定但仍可推理 —— **被出题者折算成了失效**；
- 取决于一条未定义的规则（某技术名词算不算「名字」）。

且其中一对按原设计根本不该进入该测试。

> **与 E1 的 B1 是同一类错误：分母与判据都没定义，数字却看似可读。**

替代表述：遮名产生了若干失效与不确定；**在未预注册遮除规则的条件下，具体比率不可解释。**

**② 撤回「名字是唯一可用信号」。**

出题者在同一份结论中，上一节已列出两个反例（一对靠事件性质/成因判定，一对靠日期加共同轮次序号判定），下一节仍写了「中间没有第三条路」。**反证在自己的表格里。**

修正为：**名字是高频捷径；改名处必然断裂；但事件性质、时间 + 序号等结构线索，至少在部分情况下能独立工作。**

**③ 撤回「标注者未使用给定选项 ⇒ 选项装不下」。**

向标注者确认后：原因是不想回滚页面查看选项列表，**与语义无关**。

> **这是本轮出题者犯的第三个同类错误——看到模式即为其配置理论。**

### 一项污染披露

出题者在封存答案中把第 4 对判为「同一对象改名前后」。**该判断不是从两个片段推出的**：出题者在出题前已通过检索读过相关记录，其中一条陈述了新名称，另一条使用旧名称。

因此出题者在该对上的答案**不具备「仅凭片段可判」的证据力**。这与标注者勾选「我本来就知道」属同类污染，仅来源不同。

### 由此发现的测试设计缺陷

**「我本来就知道」这一列合并了两种知识：**

| 来源 | 系统能否获得 |
|---|---|
| 标注者的亲身经历 | **永远不能**（能力上限） |
| **语料中的另一条记录** | **可以——这是召回问题** |

改名那一对属于后者。此前把这一列整体读作「系统够不着的部分」，**读窄了**。

下一版应拆两列：**第二列的占比等于「扩大召回范围能挽回多少」，是可执行的数字。**

**另有一个类别本轮完全未测**：在语料观察中识别出「同一持续状态的不同侧面」这一类材料后，**未在测试单中放入该类型的配对**。

### 撤回之后仍然成立的结论

| 结论 | 强度 |
|---|---|
| 两名标注者在配对判断上高度一致 | **弱**（不独立） |
| **非名称通道确实存在**——事件性质/成因、时间 + 序号 | 存在性，成立 |
| **跨改名处，仅凭两个片段无法判定** | 成立，两侧独立确认 |
| **该改名在语料中有显式陈述** → 属召回问题，非推断问题 | 成立，且可执行 |
| 召回小测判为「搜不到，且想不出替代办法」 | 成立 |

### 否决了什么

**① 否决了「先把判断做准，召回以后再说」这个优先级。** 此前已约定：若召回一项判为「搜不到」，则「候选怎么来」排在「判断准不准」之前。本轮结果为「搜不到」。

**② 否决了「别名归一从相似度猜测开始」。** 跨改名的桥梁**存在于语料中**（有记录明写改名），因此第一步应当是：

```text
先检索语料中显式的改名／更名／别称陈述
→ 建立别名边（带来源位置）
→ 再考虑相似度推测的别名（低置信，可拒绝）
```

并且**显式陈述来的别名**与**相似度推来的别名**必须分开记录、分开评测。

### 一个由语料观察带来的类别修正

针对某个学业/研究主题检索时，返回的多条记录彼此**不是不同的事件**，而是**同一个持续状态的多次记述**——不同侧面、不同粒度、无发生时刻、无终点。

这打破了原来的二分问法（「是不是同一件事」）：判「是」——它们根本没有事件；判「不是」——它们显然指向同一个东西。

因此把问法从二分改为五类：

| 选项 | 含义 |
|---|---|
| A | 同一件事——同一次发生的两次记述 |
| B | 同一个过程的不同阶段——连着，但不同时刻 |
| C | 同一个持续状态的不同侧面——无发生，长期如此 |
| **D** | **同一个对象，不同的事** ← 名字匹配的直接对手 |
| E | 无关 |

**D 与 A/B 的区分是这类系统的存亡点**：若「合」的机制实质上只有「名字对得上就归一起」，那么 D 恰好是名字对得上但不该归一起的情形。

**B 与 C 的边界当时就不清晰**——设计测试的人自己就有一对判不定。这不是标注难度问题，**是选项本身的设计缺陷**。

### 限制

n=6；标注者 2 人，其中 1 人为出题者；材料非随机抽样；遮名规则未预注册；所有结论均为存在性，无比例含义。

---

# 11. E9 · 切分一致性：三人独立标注

**规模：9 条材料，3 名标注者，其中 1 名是出题者且挑选了材料。**

### 要回答的问题

E7 的扩大版，外加一个具体假设：**材料的产生方式会不会影响可切分性？**

### 怎么做的

九条材料按**产生方式**分三层，**题号打乱，标注者不知道分层存在**：

- **A 层 · 抽取产物**：第三人称描述式，非本人撰写，由早期工具从原文自动抽出；
- **B 层 · 老库**：本人撰写，但当时的存储会自动合并；
- **C 层 · 新库**：本人撰写，一条一事，有空行分段。

出题者先完成切分并连同预测封存；另两名标注者独立作答，**提交前互不可见**。

除切点外，另记录「犹豫过但决定不切」的理由类型，其中一类为**「只是挨着」（同一天／同一人／同一次写下）——事先声明该类不算理由**。

### 结果 1：事前假设被否定

> 假设：一致率 C（新库） > B（老库） > A（抽取产物），理由是只有 C 层存在「作者划的边界」。

**实测相反：A 层三人几乎一致；C 层分歧最大。**

因此本轮**无法回答**「产生方式是否影响可切分性」——分层假设本身被推翻了。

### 结果 2：嵌套为主，但交叉确实存在

初稿曾断言：「**没有任何一处**是某位标注者切在了另一位认为不该切的地方；粗粒度者的切点集合**始终**是细粒度者的子集。」

把三人的切点还原到统一单元格后**逐对实算**，**该断言为假**。

九条材料 × 三对标注者 = **27 个两两配对**：

| 关系 | 数量 |
|---|---:|
| 切点集合**相同** | 9 |
| **嵌套**（一方是另一方的真子集） | 15 |
| **交叉**（互不包含） | **3** |

**三处交叉全部涉及出题者**（分布在三条不同材料上）。
**两名未参与出题的标注者之间：9 条全部为相同或嵌套，零交叉。**

切点集合的 Jaccard 均值：

| 配对 | 均值 |
|---|---:|
| 两名独立标注者之间 | **0.656** |
| 出题者 × 独立标注者甲 | 0.452 |
| 出题者 × 独立标注者乙 | 0.479 |

（实算脚本见 `reference-code/nesting_check.py`。）

**修正后的结论：**

> **在两名未参与出题的标注者之间，分歧确实只是深浅，不是位置。所有交叉都出自出题者一侧。**
> 「分歧只是粒度」这一说法，**在独立标注者之间成立，作为一般规律不成立**。

### 结果 3：排版不是边界证据（2:1 独立确认）

分歧最大的几条都是**有空行分段**的材料。

出题者顺着空行切；另外两名标注者**均未理会空行**，其中一位把一条有五个空行块的材料整体判为**一个单元**，理由是各块之间存在因果与继续关系，**空行只是排版**。

而无空行的流水句材料（A 层），三人反而**高度一致**——因为**只能靠语义判断**。

有意思的是：**决定已定而习惯未改**——此前已经决定拿掉整套结构性切分规则，但出题者在实际标注时仍沿用空行。这一点记录在案。

### 结果 4：「只是挨着」两名独立标注者均为 0 次

该类别**未被用作合并理由**，其中一位还**显式识别并拒绝**了它（「只是一起被写下来」）。出题者在封存件中承认自己使用过一次。

结论有两面：

- 该约束在人工标注中**正常工作**；
- 但也因此**无法通过人工标注来验证**——**人不会犯这个错。它是为机器写的约束，需另设验证方式。**

### 结果 5：一处真正的判准分歧

某条材料由若干彼此独立的事实构成，它们共同属于同一个上位对象。

- 一位标注者据此**不切**，理由是「这几件事指向的是同一个东西」；
- 另一位标注者**明确拒绝**同形状的理由：「同属一份文件，只是**一起被写下来**，不足以阻止切分」。

**「共享同一个上位对象／同属一份文档」算不算局部连贯关系——三人未达成一致**，且这**不是深浅问题，是判准问题**，不会随样本量增加而消失。

**这正是 E2、E3 里那个「容器 vs 核心」判据，原样出现在切分层面。**

### 结果 6：编号列表出现三种判准

两条材料含编号条目，三名标注者给出三种处理：

- 一位**两条都逐条切**，并给出可执行规则：「编号列表的共同标题／收件人只是上层容器；若每条能独立成立、彼此无具体的继续或因果关系，则逐条切」；
- 一位**两条都不切**（其中一条自述两可）；
- 出题者**一条切、一条不切**，且在封存件中已自认判准不一致。

另有一条编号材料上三人分歧最大（**1／10／1 段**）。

因此「作者显式声明的结构（标题、编号）是证据，纯排版（空行、换行）不是」这条区分**还不够**，还需第二层判据：**编号标记的是「彼此独立的条目」还是「同一行为内的列举」。**

### 否决了什么

**① 否决了「空行/换行可以作为边界证据」。** 2:1 独立确认。

**② 否决了本轮自己使用的度量。** 「段数一致率」与「切点位置一致率」都会**低估**实际一致程度。应改为检验**切点集合是否互为子集（嵌套）**——嵌套不算分歧，只算粒度不同。

**③ 否决了「分歧只是粒度」的一般化表述。**

### 一个方法缺陷：一半可证伪，一半不可

- **可证伪的一半**：交叉是可观察的，本轮观察到 3 次，因此「全部为嵌套」这一假设可被证伪，**并且已被部分证伪**。
- **仍不可证伪的一半**：对未发生交叉的配对，测试单只记录「切在哪里」，**未记录「哪里绝对不能切」**，故无法区分「没切到那个深度」与「反对在那里切」。

→ **下一版必须增加「坚决反对的切点」一栏。**

### 一个不必自己发明的东西：多粒度的度量

本轮曾把「如何度量两个切法差多少、尤其粒度不同时」当作开放问题。经检索，**该问题在文本／篇章分割领域已有专门积累**，只是不在 RAG / agent-memory 那一圈里——那一圈只做下游评测，这是前几轮调研都没撞到它的原因。

| 方向 | 内容 |
|---|---|
| **Pk / WindowDiff** | 经典惩罚型指标，对「差一点点」的边界给部分分；已知偏差是偏向边界少或边界密集的切法 |
| **Boundary Similarity / Segmentation Similarity / Boundary Edit Distance** | 针对窗口型指标缺陷提出，处理近似边界；有开源实现（该包较旧，本环境构建失败） |
| **粒度感知评测** | 已有以此为题的工作（对话分割方向） |
| **层次化评测** | 用动态规划把参考的每一层与假设的**非递减**层对齐；另有多层 boundary similarity 扩展 |

**「非递减对齐」就是「粗切点集合 ⊆ 细切点集合」的形式化版本。** 本轮从标注数据中量出的结构，在该线工作中是**建模前提**。

这些指标自陈的局限，恰好正是本轮撞上的情形：

> 它们都是**对着单一参考切分**计算的——尽管多种切分与多种粒度都可能站得住。

### 限制

n=9；3 名标注者，其中 1 人为出题者且挑选了材料；分层假设被否定；结果 2 的数字依赖出题者把三份答卷的切点**还原到统一单元格**，该还原是解读性的，可复核重算；未发生交叉的配对仍受上述方法缺陷限制。

---

# 12. E10 · 切分门的首次真实模型样本

**状态：通过但带发现 —— 管线可用于继续测试，但模型行为尚未标定。**

### 要回答的问题

不是「准确率是多少」，而是：在一小批有代表性的真实样本上，**这套流程到底怎么表现**？

- 记录是被保留、被赋予内部结构，还是被切开？
- 模型倾向过切还是欠切？
- 校验与失败回退在真实模型输出上工作吗？
- 在扩大测试之前，会先冒出哪些失败形状？

### 怎么做的

**被测的东西**：写入侧每条记录一次调用，逻辑两步、物理一遍：

```text
完整记录 + 确定性观察微单元 + 来源画像
  ↓ 一次调用
record_frame  →  micro_units[] 上下文化特征  →  gate
```

输出顺序固定，目的是减少显式门输出对前置特征的条件污染（**但同一次前向得到的特征仍不是独立证据**）。

**门是三态的，不是二分的**：

```text
PRESERVE_AS_IS                     整条保留
PRESERVE_WITH_INTERNAL_STRUCTURE   外层保留 + 内部可寻址
SEGMENT                            真的切开
```

三态而非二分，是 E9 的直接产物：**真实粒度分歧大量表现为嵌套，而不是互斥边界**。

**两条硬设计**：

- **观察微单元只是坐标，不是语义单元。** 空行不是边界；标点只用来推导确定性的句/小句坐标窗口；长度不决定切分；小节标题不自动成为父节点。（实现见 `reference-code/micro_units.py`。）
- **失败即保留。** 解析失败、位置越界、依据缺失、输出自相矛盾等，一律回退整条保留，**不丢弃、不强制切分**。

**执行**：首批 **12 条真实记录**，按归一化文本长度**分层抽样**（4 短 / 4 中 / 4 长）；补充批 **8 条**（为下一步准备候选）。

### 结果

**首批 12 条：**

| 结果 | 数量 |
|---|---:|
| `PRESERVE_AS_IS` | 8 |
| `PRESERVE_WITH_INTERNAL_STRUCTURE` | 2 |
| `SEGMENT` | **0** |
| 失败回退 | 2（均为「依据缺失」） |
| 最终语义单元 | 12 |

**补充批 8 条：**

| 结果 | 数量 |
|---|---:|
| `PRESERVE_AS_IS` | 2 |
| `PRESERVE_WITH_INTERNAL_STRUCTURE` | 4 |
| `SEGMENT` | **0** |
| 失败回退 | 2 |
| 最终语义单元 | 8 |

补充批的两次失败回退分别是：**schema 失败**（模型使用了枚举外的成分类型值）与**依据缺失**（依据的位置与字符串形状都合法，但摘录与 `raw_text[start:end]` 不完全相等）。

**20 条记录全部保留，没有一条丢失。**

**一次基础设施故障值得记**：运行器最初发送的思考开关是布尔值，服务端期望结构化对象，返回 HTTP 400。修正后成功。失败尝试报告零 token 用量。

### 人工检查发现的三个案例

| 案例 | 现象 | 判读 |
|---|---|---|
| 一条 140 字记录 | 前半讲一部影视作品，后半转向另一本小说；但**观察层把整条暴露为单个字段行微单元**，模型返回整条保留 | **粗糙的观察单元会把局部断裂藏起来**——问题可能部分在观察层，不只在模型判断 |
| 一条 581 字项目进展记录 | 含多个工作项、规则、验证任务、分支计划、反馈与后续动作，返回整条保留 | 可能确实属于同一个更大的时间容器，未必需要切；但「保留 + 内部结构」值得考虑 |
| 一条很短的规划记录 | 返回「保留 + 内部结构」，拆出两个内部成分 | 低风险，但对一条短而连贯的记录可能是**多余结构** |

**另一项人工观察**：两个「保留 + 内部结构」型响应里，模型倾向于**把引号规范化**，而不是逐字复制原文。

重要：这两次被拒响应的**正式校验结果是「依据缺失」**，所以引号规范化**不得**被记成官方失败原因。它作为独立的人工观察留待复现——而它后来确实在 E12 复现了。

### 否决了什么

**① 否决了「跑完切分就自动跑完备配对」这个做法。**

导入还从 12 个单元自动生成了 **66 个**关系判定提示（全部两两组合），但**没有发送**。理由：12 条互不相关记录的任意配对，绝大多数只会反复测「无关」，信息量极低。

> **自动生成的完备配对不等于有信息的测试集。**

正确做法是**刻意构造** 10–15 对，覆盖：同一件事 / 同一过程的不同阶段 / 同一持续状态的不同侧面 / 同一对象但不同事 / 明显无关 / 真正模糊。

**② 没有否决「保留优先」，但把它标成了未标定。** 0 次切分不自动是错的——这套门本就设计成保留优先，举证责任在「切」这一侧。但**它也意味着切分能力完全没有被验证过**。

### 待跟进的六项

- **F1** 真实记录上可能存在保留 / 欠结构偏置；
- **F2** 粗粒度的字段行观察会遮蔽内部的局部断裂；
- **F3** 短而连贯的记录可能获得不必要的内部结构；
- **F4** 真实输出会失败于严格的来源依据校验；
- **F5** 模型可能规范化复制证据中的标点，暂列为人工观察；
- **F6** 20 条只是**诊断样本，不是准确率基准**。

### 限制

20 条；单一 provider；单一提示版本；分层抽样但非随机；没有对照组（例如换一种微单元切法再跑一次）。

---

# 13. E11 / E12 · 关系判定的两轮真实样本

### 要回答的问题

在**正确候选已经摆在模型面前**的条件下（即把召回问题排除掉），模型能否判对两个单元之间的关系类型？

### 被测的标注体系

关系被表示成**两个独立维度**：

```text
anchor_kind:  EVENT | PROCESS | STANDING_STATE | ENTITY_OR_PROJECT | NONE | UNCLEAR
relation:     SAME | STAGE_OF | ASPECT_OF | SAME_REFERENT_DIFFERENT_EVENT | UNRELATED | UNCLEAR
```

两维可各自独立标 `UNCLEAR`。**关系维里没有泛化的 `RELATED`**——这是刻意的：一旦有了这么一个筐，所有说不清的东西都会掉进去，边界就靠约定维持，而约定会漂移。

**这四种关系不是一把从严到松的刻度，是不同类型。** `SAME_REFERENT_DIFFERENT_EVENT` 是「**不是同一件事**」的**正面判定**，不是「更松一点的同一」。

> 这一点是从一次自查错误里来的：曾经把四种关系讲成「从严到松的一把刻度」，那样滑到最松会把**已经被正面判定为不是同一件事**的对合并掉。而且同一个错误在实现侧又犯了一次（提议对全部关系类型做连通分量）。**概念上改对了，代码里没跟着改。**

### 证据契约

任何被接受的关系都必须带**可回指原文**的证据。实现见 `reference-code/b_judge.py`。

---

## 13.1 E11 · 第一轮

### 怎么做的

10 对人工金标准**在看到任何模型输出之前锁定**，覆盖 `SAME / STAGE_OF / SAME_REFERENT_DIFFERENT_EVENT / UNRELATED / UNCLEAR`。`ASPECT_OF` 本轮刻意不覆盖。

### 结果 0：尝试 0 是契约失败，不计分

10 个提示里 **9 个**返回 HTTP 400：

```text
Prompt must contain the word 'json' in some form to use 'response_format' of type 'json_object'.
```

**根因**：运行器正确请求了 JSON 模式，但当时的提示没有显式要求 JSON 输出、也没有写出响应的 JSON 形状。**这是提示/provider 契约缺陷，不是语义结果。**

处理方式值得记录：那唯一 1 次成功的响应**也不得计分**，因为它是在一个「在当前 provider 契约下并非一致有效」的提示集下产生的。修正提示后，**用同一批锁定配对重新生成全部 10 个提示、重跑全部 10 次调用**，只对修正后的运行计分。

修正原则：**不要仅仅把 `json` 这个词注入进去当作绕过手段，而要把契约写显式、写得可测。**

### 结果 1：原始语义判断（尚未过证据校验）

| 配对 | 人工金标准 | 模型 | |
|---|---|---|---|
| 1 | SAME | SAME | ✓ |
| 2 | STAGE_OF | STAGE_OF | ✓ |
| 3 | STAGE_OF | SAME_REFERENT_DIFFERENT_EVENT | ✗ |
| 4 | SAME_REFERENT_DIFFERENT_EVENT | UNRELATED | ✗ |
| 5 | SAME_REFERENT_DIFFERENT_EVENT | SAME_REFERENT_DIFFERENT_EVENT | ✓ |
| 6 | UNRELATED | UNRELATED | ✓ |
| 7 | UNRELATED | UNRELATED | ✓ |
| 8 | UNRELATED | UNRELATED | ✓ |
| 9 | UNCLEAR | UNRELATED | 分歧 |
| 10 | UNCLEAR | SAME_REFERENT_DIFFERENT_EVENT | 分歧 |

**原始一致：6/10。** 分片：高置信金标准 6/7；高 + 中高置信 6/8；显式负例 **3/3**；`UNCLEAR` 使用 **0/2**。

模型的原始关系分布：SAME 1 / STAGE_OF 1 / SAME_REFERENT_DIFFERENT_EVENT 3 / UNRELATED 5 / ASPECT_OF 0 / **UNCLEAR 0**。

`anchor_kind` 本轮未设人工金标准，故不计分，分布仅作观察保留。

### 结果 2：正式的来源回指校验

把尝试 1 通过现成的解析器与来源校验器导入，使用真实记录、真实语义单元与单元位置。**没有编辑任何模型输出，也没有手工修补任何证据位置。**

| 结果 | 数量 |
|---|---:|
| 接受 | 9 |
| 失败回退 | 1（证据摘录不匹配） |

被拒的那一条是**真实的模型证据错误**，不是测试装置缺陷：提交的 b 侧摘录与给定位置对应的真实来源切片不符。校验器正确地把该主张降级为「不确定」。

- 原始关系一致：**6/10**
- **校验后关系一致：5/10**

**从 6/10 降到 5/10 完全归因于那一次证据失败，必须与语义误判分开记录。**

### 误差分解

**语义分歧：**

- 一个「同一过程的不同阶段」被判成「同一对象不同事件」；
- 一个高置信「同一对象不同事件」被判成「无关」——**这是漏掉同一所指关系，不只是邻近类型混淆**；
- 两个中置信「不确定」都被模型**强行给了确定标签**。

**证据/契约拒绝：** 一个语义上本来匹配的响应，因摘录不精确而失败。

---

## 13.2 E12 · 第二轮

### 怎么做的

针对第一轮暴露的四个弱边界重做 10 对锁定配对。**刻意不是扩大随机样本**——第一轮已经证明随机配对绝大多数只重复测「无关」。

### 结果

| 指标 | 结果 |
|---|---|
| 原始关系一致 | **7/10** |
| 校验后关系一致 | **6/10** |
| 高置信校验后一致 | **6/8** |
| 来源回指校验 | 9 接受 / 1 失败回退 |
| `STAGE_OF` | 2/3 |
| 硬负例 `UNRELATED` | **4/4** |
| `UNCLEAR` | **0/2** |
| `ASPECT_OF` | **未验证** |

**误差分解：**

- 语义分歧三处：一个「同一过程的不同阶段」→「同一对象不同事件」；两个「不确定」→ 分别被给成「同一对象不同事件」和「无关」；
- 证据拒绝一处：**模型把直引号规范化成了弯引号**，于是提交的摘录与来源切片不完全相等。

**校验器行为正确，两轮都没有发现校验器或测试装置的缺陷。**

`ASPECT_OF` 反复定向搜索都没有找到一个干净的真实正例，因此**既不报通过也不报失败**，明确记为未验证。**不为了凑覆盖率而制造样本。**

---

## 13.3 跨两轮的五条发现

1. **干净负例的区分能力一直很强**（3/3、4/4）。
2. **「同一过程的不同阶段」与「同一对象不同事件」的边界是主要语义混淆点。** 这正是 E8 里 D 与 A/B 的分界——「合」机制的存亡点。
3. **模型系统性回避「不确定」，可复现。** 两轮合计，在 **4 个刻意做成模糊**的人工金标准上，模型选择「不确定」的次数是 **0/4**。
4. **要求模型逐字复制原文摘录，在工程上是脆的。** 引号/标点规范化会把一个语义正确的判断打成失败。
5. **校验器/装置在观察到的证据失败中行为正确。**

## 13.4 否决了什么

**① 否决了「让模型逐字复制证据原文」这个契约。** 见 E13。

**② 否决了「靠提示让模型自己使用不确定」这个假设。** 0/4 且可复现。产品要么在提示层强化，要么**接受一个果断的模型，并把不确定性留给确定性拒绝路径**。

**③ 否决了「继续扩大随机配对样本」。** 两轮都表明信息来自**刻意构造的边界样本**，不是样本量。

**④ 否决了「用一个数报关系判定质量」。** 原始一致、校验后一致、按置信度分片、按关系类型分片——这四个数说的不是一件事。合成一个数会把证据契约问题读成语义能力问题。

### 限制

每轮 10 对；单一 provider；金标准由参与设计的人锁定（虽然锁定在前，但不是第三方）；`ASPECT_OF` 完全未覆盖；`anchor_kind` 维从未设金标准。

---

# 14. E13 · 三项契约修复（离线，无模型调用）

两轮之后确立的三项跟进，**全部离线完成，没有再跑一次 provider 实验**。

这本身是一条纪律的执行：**严格性应当与「决策错了的代价」成正比，而不是与好奇心成正比。**

### 14.1 证据契约：从「复制文本」改为「给位置」

**问题**：要求模型同时返回 `{start, end, excerpt}` 并逐字匹配，等于**让排版规范化拥有否决语义判断的权力**。

**修正**：模型现在只返回 `(side, start, end)` —— 哪一侧、哪个位置。

- 导入器仍然严格校验：侧别正确、位置在记录范围内、**位置落在该语义单元自己的来源片段内**（防止同源切分的两个单元互相拿对方的证据）；
- 规范摘录与来源信息由导入器**从真实记录确定性绑定**；
- 旧响应里残留的 `excerpt` 字段被**有意忽略**。

> **模型给语义判断和位置，系统给证据文本和出处。模型永远不复制出处元数据或权威原文。**

顺带确立的一条：**覆盖检查按「侧别」而不是按「来源标识」**——来自同一条记录的两个切分单元也必须两侧各有证据。

### 14.2 不确定性标定：只改提示，不重跑

提示现在明确写出：证据不足时必须返回「不确定」，返回一个关系不要求强行确定。关系枚举与失败回退行为不变。

**已有的 0/4 真实发现继续挂在观察名下**——仅仅为了验证一次提示改动而重跑 provider，不划算。

### 14.3 运行器：并发、预算与可恢复

加上有界并发、可配置推理力度、可选的每请求完成 token 预算，以及跳过已持久化成功项的续跑。伪客户端测试覆盖并发上界、参数透传、有序持久化、失败处理与并发续跑。**本次改动没有调用任何 provider。**

一个关键细节：导出的请求 ID **绑定到确切的提示契约**（切分阶段还绑定来源出现位置），因此续跑**不可能**静默复用一个在旧证据/提示 schema 或已变更来源卡片下产生的「成功」结果。

---

# 15. E14 · 噪声底：同一合同重复采样

**这一项推翻了前面所有单次分数的读法，包括 E11 / E12 自己的。**

### 要回答的问题

E11 与 E12 之间，关系判定的分数从 5/10 变到 6/10。**这个 ±1 是效果还是噪声？**

触发这个问题的是一次异常：两轮之间提示词只改了两处（把摘录移出证据契约、加强不确定性措辞），**两处都与关系语义无关**，却有一个案例从判对翻成判错。

只有两种解释，且都指向同一个动作：**要么单次运行存在 ≥1/10 的噪声底，要么模型对无关的提示词文本高度敏感。** 无论哪种，**在噪声底未知之前，任何 ±1 的读数都不可解释。**

### 怎么做的

同一批锁定配对、**完全相同的提示词与 provider 参数**、不复用任何历史响应，重跑 3 次；与已有的 1 次合并，得到每个案例 **4 个同合同观测**。

观测单位从「一次标签」升级为 **`(锚点类型, 关系)` 联合元组**——此前两轮的已发布报告**只记了关系那一维**。

### 结果

| 指标 | 结果 |
|---|---|
| 联合元组 4/4 完全稳定的案例 | **0 / 10** |
| 众数集中度 | 3/4 出现 3 例；2/4 出现 6 例；**1/4 出现 1 例**（四次四种答案） |
| `PROCESS` 锚点出现次数 | **1 / 40** |
| 配对合法性 | canonical 23 / 非 canonical 7 / **逻辑互斥 10** |
| 硬负例的关系稳定性 | 13 / 16 |
| 负例金标准上产生的假边 | **6 / 24** |
| 三次重复的关系命中 | 6/10、5/10、5/10（provider 失败 0） |

### 否决了什么

**① 否决了单次 10 次调用的可归因性。** 同一个案例在完全相同的合同下会进入多个不同的联合状态，而且这些漂移**会改变最终图上的边**。此后所有对照都改成**同期配对 A/B**，而不是「今天跑 A、改天跑 B 再比分布」。

**② 否决了「校验器 40/40 全通过」的含义。** 40 个观测里 17 个不是 canonical 配对，其中 **10 个逻辑互斥**——例如锚点断言两侧共享**同一个事件**、关系同时断言两侧**不是同一事件**。校验器逐个枚举合法即放行，没有任何交叉约束。

> **「全部通过校验」只说明线格式与来源契约通过，不说明这条判断内部自洽。**

**③ 否决了以准确率作为后续实验的判据。** 联合稳定性 0/10 的前提下，n=10 的分数几乎没有分辨力：6/10 → 7/10 与噪声完全不可分。判据改成**机制指标 + 危害指标**：

```text
机制：PROCESS 锚是否出现（基线 1/40）
      配对三分类（canonical / 非 canonical / 逻辑互斥）
      重复采样下联合状态的集中度
危害：负例金标准上的假边数（基线 6/24）
      仅以裸专名为证据的正例断言数（基线 1/40）
```

### 三条附带发现

**① 重复之间存在「整轮情绪」，不是逐题独立。** 「不确定」这个取值在**单一一轮里聚集**（一轮中锚点出现 4 次、关系出现 3 次；另一轮各 1 次；其余两轮 0 次）。若逐题独立抽样不该这样聚集。

**后果**：3 次重复的有效样本量小于 30——整轮会一起偏保守或一起偏武断。这直接支持后续改用**同期配对**设计（轮次情绪被差分掉），并说明非配对的分布比较即使有 30 次调用也不可靠。

**② 一次撤回**：此前从两轮之间的比较推出「锚点漂移是**定向**的」。拿到 4 个同合同观测后作废——**两点之间画不出方向。**

**③ 关系稳定的只有硬负例。** 两轮之间完全稳定的只有硬负例的关系（8/8），其余每一类都动过。

### 限制

10 个案例、4 个观测；单一 provider；这些数只描述该合同下的稳定性，**不构成对模型能力的度量**。

---

# 16. E15 · 卡片修复：一次结果为 MIXED 的对照

### 要回答的问题

E11 / E12 把「同一过程的不同阶段」与「同一对象不同事件」的混淆归因于模型。**但送进模型的卡片长什么样，此前从没有人看过。**

拿到原始请求件后逐张检查，发现三处：

| 发现 | 数据 |
|---|---|
| 卡片上唯一被标注为「锚点」的字段，取的是**项目容器名** | 全部 |
| 记录级的**过程字段**在物化时被丢弃，从未进入卡片 | 全部 |
| `时间` 与`参与者`两栏恒为 `none` | **20 / 20** |
| 有 4 张卡**所有结构化字段全为 `none`**（上游切分失败回退所致），只剩原文 | 参与 10 对中的 3 对 |

以及一处提示词侧的：**六个关系标签里只有「同一件事」被定义过**，其余三个从未在提示词中给出任何定义，模型只能按英文字面选择。

> 在这之上再写一句「请选择最有解释力的共同锚点」，**等于要求模型反驳我们自己提供的卡片**。

### 怎么做的

只改卡片装配：把上游已抽出的**记录级过程字段**传到语义单元、补齐保留路径的上下文字段。**不动提示词、不动关系分类、不动 schema、不动金标准。**

设计为**同期配对 A/B**：3 个区块 × 10 个案例 × 两侧 = 60 次全新调用，案例级顺序交替，不复用。

主判据**刻意不是准确率**，而是**证据跨度的内容类型分布**——把每条证据分为「内容级」（含谓词的活动/约定/状态变化）/「裸名称级」（人名、项目名、日期抬头、不透明标识）/「碎片」（切断可读语义单位）。

### 为什么用证据类型当主判据

因为 E14 之后做了一次跨观测的证据解析，得到一个更靠近机制的发现：

| 某案例四次观测的证据落点 | 输出 |
|---|---|
| **过程性内容**（一侧的一轮修改 → 另一侧的复测） | 2 次，**全部 canonical 且正确** |
| **裸项目名** | 2 次，**全部逻辑互斥且错误** |

**没有交叉。** 同一个案例、同一张卡，指向内容 → 判对；指向名称 → 判错。

由此把随机源前移：

> **真正不稳定的是证据选择；关系只是它的下游。内容级证据对关系有强约束，裸名称级证据使关系处于欠约束状态。**

（此处也有一次撤回：起初写成「关系是证据选择的确定函数」，被一个四次证据同区域却给出三种关系的反例否掉。）

### 结果

**`MIXED`，且逆转了唯一一个可重复判对的正例。** 那个案例在修复前 3/3 判对，修复后 3/3 判错——**而它的证据已经落在正确的过程内容上**。

**同时，对照组被自己的实现毁掉了。** 卡片渲染无条件打印过程字段那一行、空值也印 `none`，于是**空白卡的请求内容也变了**，10/10 全部被划进「受试组」，**预先设计的不变量对照组是 0**。

> 这条已登记未修。**读这次结果时必须带上它**：这一轮实际上没有同期对照。

### 否决了什么

**① 否决了「问题主要在模型对标签的理解」。** 给定正确的过程性证据，模型两次都产出了 canonical 的正确判断——**它知道那个标签是什么意思。**

**② 否决了「补上被丢弃的字段就能改善关系判定」。** 修复是**加法而非替换**：旧的名称型锚点仍在卡片上，也没有告诉模型该优先看哪一个。结果是多了一个吸引子，而不是移走了旧的那个。

**③ 由此收窄了缺口的位置。** 已经找到共享过程、证据也对，最后仍落到「同一对象不同事件」——说明该取值正在充当**默认落点**。缺口不在「什么算过程连续」的定义，而在**「已经确认过程连续时，为什么仍可以选它」**。

### 一条不受此影响的独立发现

某个硬负例产出了一条假边，进入最终图。解析证据后：**两侧的证据各自只有一个人名，没有任何其他内容。**

而当时的提示词里明确写着：

```text
- Same person/project/tag alone is NOT evidence.
```

**规则写了，模型违反了，校验器放行了（位置在界内即通过），断言落库并连边。**

> **提示词里的禁令不产生任何可执行约束。** 现有三层（提示词规则 / schema 校验 / 来源回指校验）**没有任何一层**能拦住「证据只有一个专名」。

### 一条被移除的自洽检查

E13 把摘录移出证据契约是对的（引号规范化会造成伪失败）。但摘录同时还承担了第二个功能——

> **它是唯一能证明「模型知道那个偏移处是什么文本」的自洽检查。**

移除后，**任何在界内的整数对都同样有效**。实际解析发现相当一部分位置跨词或跨标点：起点落在句号上、切掉专名首字、在括号中间断开；同一处内容在不同观测间边界漂 1–5 个字符。

**这属于修一个问题时顺手丢掉的信号**，且当时无人注意到。

### 限制

对照组失效；主判据的分类需要看私有正文，因此只能由材料拥有者本地盲分类后回报计数；证据在案例内与轮次内聚集、不独立，因此一切结论只作描述性对比，**不做显著性主张**。

---

# 17. E16 · 第一屏：人机并排与「选择性追溯」

**这是唯一一次直接测「这套东西对读的人有没有用」。**

### 要回答的问题

前面所有实验测的都是「判得准不准」。这一项测的是那个从未被回答、且决定整条链正当性的问题：

> **机器整理出来的结构，对真正在读的人是否更有用？**

### 怎么做的

**材料**：连续机械选取的一批真实记录（两天，12 条），不挑选。

**双盲并排**：先跑完机器侧并**封存**；材料拥有者在**完全看不到机器结果**的前提下，只按自己的阅读习惯标出真正有助于理解这批记录的连接与简短说明；两边都完成后再开封比较。

机器侧：切分阶段 12 次尝试 9 条落盘、3 条因外层超时按失败保留处理；关系判定 24/24 全新调用。

### 结果一：`价值混合`

**正面的一条是真的**：机器确实找到了若干**人工没有手动画出、但回看后被认可为有理由**的联系。

> **所以关系判定不是纯分类器，它具备一定的发现能力。** 这一点不应被后面的否定结论盖掉。

**但产品侧的问题出在别处：**

- 这批材料里存在大量**认知/反思的重复出现**，而不是单一事件；
- 多条局部边分别都可能合理，但**连通分量投影会把不同维度的合理关系传递成越来越大的块**；
- 人工样张本身也是重叠网络，并未穷举——材料拥有者估计**仍有约四分之一的紧密联系没有画出**。

因此：

> **「找全真实关系」既不现实，也没有被证明对追溯有用。**
> **提高关系判定的精度，不会自动解决「信息量仍然过大」这个问题。**

### 人工侧自己的观察：连线画不满，而且「画满」本身没有意义

人工样张不是被时间限制卡住的半成品，**是画到某处画不下去了**：

- 材料拥有者对这 12 条逐一尝试连线后，发现**以人的标准，它们无法被全部「明显地」关联起来**——一部分记录之间确实有关系，但那种关系弱到写不出一句能让第二个人认可的理由；
- 因此**弱联系被有意留空**：不是漏了，是判断「画上去也不构成可用的连接」；
- 前面所说的「自陈约四分之一未画出」，指的正是这一类，而不是画错或画少。

> **这是一条比机器结果更早的负面证据：连接关系的「完整版」在人这一侧就不存在。**
> 机器侧再准，也没有一个可以对齐的完整答案。

### 由此得到的直接放弃理由：弱边与集合分离不可兼得

截止线之前的那套方案**是在意弱边的**——弱边正是「同一件事的不同侧面」「同一条线的前后阶段」这类关系的主要形态，砍掉弱边等于把设计意图砍掉一半。

但把弱边留下之后，出现的是一个结构性冲突：

| | |
|---|---|
| 保留弱边 | 边的数量迅速上去，几乎任意两个区域之间都存在若干条「说得通」的弱边 |
| 而集合之间**必须允许重叠** | 所以不能靠「一条记录只能属于一个集合」来切断传递 |
| 两者相加 | **没有一个位置可以把不同集合干净地分开**——切在哪里都会剪掉一批本来合理的弱边，或者放过一批把两个集合粘起来的弱边 |

这跟前面「连通分量越投影越大」是同一个现象的两种说法：那边说的是**结果**（块糊掉了），这边说的是**为什么修不好**（能用来修的两个手段——删弱边、禁重叠——各自都会破坏设计本身）。

> **结论：不是弱边判得不准，也不是阈值没调好。是「保留弱边」与「集合可分」在这个设定下互相排斥。**
> 截止线前的这条路线在这里停止，不是因为某一项指标不达标。

### 结果二：选择性追溯 —— 不成立

由结果一提出的替代方向：不再追求全局找全关系，而是**全局形成宽松可重叠的大区域；给定一次具体的追溯意图后，只在相关区域里挑少量最值得继续看的记录**（每次最多 5 条，且必须能回原文）。

用同一批 12 条记录、两个从人工样张里自然浮现的追溯意图做最小对照。

| 结果 | |
|---|---|
| 能否把候选压到 ≤5 条 | **能** |
| 意图 A：唯一一条明确「值得下一步看」的记录 | **被稳定排到第 4–5 位** |
| 意图 B | **没有任何一条被判为「值得下一步看」** |

**结论：不支持。**

> **「语义相关 / 区域结构会自然推出下一步该读什么」这个假设，在这批材料上不成立。**

### 否决了什么

**① 否决了「把关系判定继续调准」这条路线的优先级。** 边界精度不是当前瓶颈——信息量才是。后续的关系边界校准与更深的提示词实验全部暂停。

**② 否决了把连通分量当作最终产品形态。** 局部边各自合理 → 投影出来的块越来越大，**这是传递造成的糊化，不是判错造成的**。

**③ 暴露了三个此前被当成同一件事的概念：**

```text
语义相关   ≠   长期重要   ≠   追溯 / 导航价值
```

在项目演化的追溯里，**阶段状态 / 里程碑式的记录，可能具有远高于普通相关记录的历史重建价值**——而它们在语义相关度上未必突出。

**④ 提出了一个产品边界问题**（记录为待讨论，未裁定）：这套聚合是否已经把系统推向了它原本明确拒绝承担的**分类器**职责。

### 一条重要的方法学限定

这批 12 条是**连续机械选取的两天**，偏反思、几乎没有事件可串。

> 这种材料上，**基于字面共现的笨办法占便宜**（互相引用、带编号），**基于关系判断的方法吃亏**。
> **用同一批材料同时评两种方法时，量到的有一部分是材料性质，不是方法差异。**

### 限制

12 条；单一批次；人工侧只有一人（材料拥有者本人，且是唯一有文本外知识的人）；人工样张自陈不完整（约四分之一未画出，且其中弱联系是有意留空的，见上）；两个追溯意图均由同一份人工样张导出。

**这一节里「弱边与集合分离不可兼得」那一条，证据等级是维护者观察，不是量化结果**：边数没有统计，「干净分割不存在」也没有用任何图指标验证过。它的作用是记录**这条路线实际停在哪里、为什么停**，不能当作一般性的不可能性论证。

---

# 18. 关于切分的三条横跨结论

这三条都不是某一项实验单独得出的，而是横跨几项之后才成形，因此单独列在这里。

## 18.1 为什么不用脚本切

「按换行、空行、句号、字数切」是最省事的做法，也是最早被采用的做法。它被拿掉，有四条独立的理由。

### ① 排版信号在这个语料上**两个方向同时错**

这是最要命的一点——它不是「不够准」，而是**在该切的地方不动、在不该切的地方动**：

| 方向 | 证据 |
|---|---|
| **不该切的地方切了** | E7：封存的预测里写「记录 B 可能被按空行切成三块」，实测两名标注者都没切。当时的原话是**只有旧代码会被空行误导，人不会** |
| **该切的地方不动** | E4：结构基线 `S0` 把 30 例**全部**判成「不切」——包括 12 例明确该切的 |

一个只在一个方向出错的信号还能靠调阈值救；**两个方向同时出错的信号没有阈值可调**。

### ② 人不用它，而且是独立确认的

E9 里分歧最大的几条恰恰都是**有空行分段**的材料。

- 顺着空行切的只有出题者一人；
- 另外两名标注者**均未理会空行**，其中一位把一条有五个空行块的材料整体判为**一个单元**，理由是各块之间存在因果与继续关系——**空行只是排版**；
- 而无空行的流水句材料，三人反而**高度一致**，因为**只能靠语义判断**。

**2:1 独立确认。** 「空行是作者划的主题边界，不该跨过它合并」这条因此被撤回。

### ③ 它把两种证据强度完全不同的东西混成一类

脚本看到的都是字符，但这两者不是一回事：

```text
标题、编号  = 作者显式声明的结构   → 是证据
空行、换行  = 纯排版               → 不是证据
```

E9 的数据部分支持这个区分：编号材料上三人的段数比空行材料一致得多。

**但即使是显式结构，也不够。** 同一轮里有一条编号材料，三人给出 **1 / 10 / 1 段**——分歧最大的一条正是编号材料。三人对编号的处理给出三种判准，其中一位提出了唯一可执行的那条：

> 编号列表的共同标题／收件人只是**上层容器**；若每条能独立成立、彼此无具体的继续或因果关系，则逐条切。

这需要第二层判断——**编号标记的是「彼此独立的条目」还是「同一行为内的列举」**。脚本给不出这一层。

### ④ 顺带暴露的一个工程教训

旧原型的切分注释里写着「轴不一致就切」这条触发条件，**代码里根本没实现**。也就是说，关于这个脚本切分器行为的讨论，有一段时间是在讨论一份不存在的实现。

> 这不是切分特有的问题，但它在结构切分这种「看起来一目了然」的代码里最容易发生。

### 结论：脚本没有被丢掉，是被**降级**了

它不再产生边界，改为产生**观察坐标**：

```text
确定性       同输入 → 同输出，可缓存、可复核
稳定的位置   句 / 小句 / 编号条目 / 字段行 / 标题各自的起止偏移
什么都不宣布 空行不是边界；标点只用来推导坐标窗口；
             长度不决定切分；小节标题不自动成为父节点
```

这是脚本**不可替代**的部分——语义判断没法给你一个稳定、可缓存、能逐字回指的坐标系。

**把「定位」和「判断」分开之后，两边都变好了**：坐标系不必假装懂语义，语义判断也不必自己去数字符。

（实现见 `reference-code/micro_units.py` 与 `reference-code/normalization.py`。）

**但降级本身也有代价**，E10 记到了：一条 140 字、内容明显转折的记录被观察层暴露为**单个字段行微单元**，结果把内部断裂**藏了起来**。坐标太粗，后面的判断就看不见该看的地方。**这条至今没有做过对照实验。**

## 18.2 提示词是切分行为的主开关

> **证据等级：维护者观察。未量化、无对照实验。**
> 之所以仍然记进来，是因为它直接决定了下面那些数字该怎么读。

维护者在实际让模型直接做切分时反复观察到：**随着提示词写法不同，模型容易落到两个极端——要么过度切分，要么完全不切。中间地带不稳定。**

这条观察改变了报告里一个数字的读法。

E10 的真实样本是 **0 次切分**，强保留偏置。但那一版提示**把「不切」设成默认、把举证责任放在「切」这一侧**。因此：

> **0 次切分不是关于模型能力的结论，而是关于那一端提示词的结论。**

**另一端从未被正式跑过。**

由此得到三条实际约束：

**① 任何「模型切了多少」的数字，必须连同提示词那一端一起报。** 脱离提示词写法的切分率没有意义，跨提示版本比较切分率更没有意义。

**② 这解释了为什么一致率类指标在这里彻底失效。** 两个极端**各自与自身完全一致**：

- 「从不切」在任何一致率指标上都打不倒——它与自己永远一致；
- 「全都切」同样如此。

这与 E3 里那对候选是**同一个失败形状**：旧提示词从不弃权、新候选一律弃权，两个都能在一半的题上「永远对」。

> **这是同一个反复出现的失败模式：分不出「做对了」和「它总是这么答」。**（见 §20.3）

**③ 因此唯一还站得住的硬约束是不对称的那条**：

```text
人明确说不该切的位置，机器切了 = 失败
```

它不依赖切分率，也不会被任何一个极端骗过去。

**这条观察本身也需要被验证。** 最小对照是：同一批记录跑两版提示（举证责任分别在「切」和「不切」），只看切点总数与位置分布的差异。**成本很低，但从未做过。**

## 18.3 为什么要先切再合

「切」和「合」是两个动作，顺序不是随便定的。但**理由不是「切完更好合」**——那个说法在 E7 已经被否决过一次（见下）。真正的理由更硬。

### ① 不切的话，「合」在判一个混合物

一条装着几件不相干的事的记录，整条进入聚合时就是一个**混合物**。对它做任何判断都会失去定义：

- 判「和另一条是同一件事」——**是哪一部分同一？**
- 判「不是同一件事」——**又是哪一部分不同？**

这不是精度问题，是**问法本身没有定义**。

而「一条记录装着几件事」在这个语料里**不是假设，是明写的观察**：

| 证据 | 来源 |
|---|---|
| 一条记录标题自称「合并桶」，而它记载的第一条规则恰恰要求「每条只写一件事」 | E7 |
| 又一条把**两个不同日期**的工作进展合在一条内，以日期分段 | 语料观察 |
| **作者本人**直接确认：某条材料里有一段是后来被追加进去的 | E9 |

此前「部分多事件记录来自写入时的自动合并」只是推测，**这三条把它升级成了观察到的事实**——但**比例仍然未知**。

### ② 长记录的向量是几件事的平均

> **证据等级：维护者观察 + 结构性推理。这一条从未被直接测量过。**

如果一条混合记录整条只有一个向量，那个向量是这几件事的某种平均：

- **它不靠近其中任何一件** → 该找到它的时候找不到；
- **它也没有区分力**（离什么都不太远）→ 不该找到它的时候反而可能被捞出来。

**两个方向同时坏——和 §18.1 里排版信号的毛病是同一个形状。**

而 E5 的长度普查说明这个问题**正好落在最重的那部分**：200 字以上只占约 **10%** 的记录，却装着**一半以上的文字**。按条数看长记录是少数，**按内容看它们是主体**。

这**可能**也是 E1「向量里没有密度簇」的一个机制。但请注意分寸：E1 测的是**全局结果**，它不能证明原因就是这个。**把两者读成因果关系是过度解释。** 真正能验证这一条的对照是把长记录整条的向量与它切开后各单元的向量分别比到各自最近邻的距离，成本很低，**至今没跑**。

### ③ 同一个毛病在聚合侧原样再出现一次

混合物进了候选组之后，**判「同一」和判「不同」都不对**：

- 判同一 → 把另外几件不相干的事**一起拖进来**；
- 判不同 → **丢掉**里面真正相关的那一件。

**这不是推演。** E2 真实卡组那一轮的宽主题簇正是这么形成的，并直接导致了三种失败：反向对齐、关系容器被当成核心、宽主题四舍五入。那一轮的结论——

> **应当退回聚类层去减少宽主题簇，而不是继续在判断层加反例**

——说的就是这件事：**上游没切干净，下游怎么调都是在给混合物贴标签。**

E8 里那个从二分改五类的修正是同一问题的另一面：**当材料本身是混合的，二分问法的两个答案都不对。**

### ④ 但结论不是「多切」

E9 和 E10 从另一侧把它夹住了：

- 人对同一材料给出**不同深度且全部站得住**（27 个配对里 15 个是嵌套）；
- 而一旦把举证责任放到「切」那一侧，模型会走到另一个极端（§18.2）。

所以「先切再合」的正确表述**不是**「切得越细越好」，而是：

> **切只负责一件事——把「几件事被写在一起」的情况拆开，让「合」拿到的输入是一个可判断的对象。**
>
> **它不负责找到唯一正确的粒度，也不承诺切完就自然成组。**

后半句是 E7 明确否决过的：曾经的前提是「底层的合会被切消化掉——只要切得干净，同类的段自然落在一起」。实测否决后改为：

```text
「切」生成更干净的叶子；
「合」是跨叶子识别同一事件／过程。
切显著降低合的难度，但不消灭它。
```

三个可执行的推论：

1. **举证责任在「切」这一侧**——只在存在可回指原文的局部断裂时才切；
2. **「保留 + 内部可寻址」是第三态**——正是为了「外层确实是一个容器，但内部要能单独指到」这种情况（也承接 E9 量出的嵌套形状）；
3. **切的验收指标不是切分率**，而是「人明确说不该切的地方，机器切了没有」。

---

# 19. 被否决的做法：汇总

**每一条都注明是「在这个语料上被否」还是「一般性地被否」。**

### 19.1 相似度与自动成组

| 做法 | 结论 | 依据 | 范围 |
|---|---|---|---|
| 全局 embedding + 密度聚类发现结构 | 否决 | E1：silhouette 0.155–0.165 | **本语料** |
| 让聚类结果自动成为语义父节点 | 否决 | E1 分析：偶然组合被固化；小簇最易产生空泛标题 | **一般** |
| 靠调判断层的提示词去救宽主题簇 | 否决 | E2：迭代 9 版，最后两版之间只有 2 处翻转，且都没到位 | **一般** |
| 一套相似度同时回答「同一吗/有关吗/属于什么」 | 否决 | 全程 | **一般** |
| 万能加权总分（人物 20% + 主题 40% + …） | 从未采用 | 同一信号在不同层级意义不同；压成一个分数后无法回指、无法反驳 | **一般** |
| 让相似度分数直接宣布同一性 | 否决 | 分数**没法被拒绝** | **一般** |
| 「组件优先」（先逐条抽成分再聚合） | 否决 | E3：8/12，4 张正例全部弃权，4 处回归 | **该任务上** |
| 用「成分重合了多少」当成核判据 | 否决 | E3 后续分析：真锚点与硬负例的支持数**完全重合**（3/5 对 3 条），判别力全在「重合发生在哪个维度」上 | **一般** |
| 从「拒绝理由」反推模型能力 | 否决 | 补遗 S5：4 张硬负例有 2 张是被**笼统理由**拒的，分不出「拒得准」和「什么都拒」 | **一般** |

### 19.2 容器类线索（这条线贯穿 E2 / E3 / E9）

| 线索 | 结论 | 依据 |
|---|---|---|
| **同一批人** | 不能单独成为共同核心 | E3 人物容器诱饵；E2 反例 |
| **同一段关系** | 不能单独成为共同核心 | E2：模型把关系当具体物，3/5 误判为核心；E3 关系容器诱饵 |
| **同一种行为模式 / 同一个持续变化** | 不能单独成为共同核心 | E3 两类诱饵；早期提示词曾把它们列为可接受核心，后被删掉 |
| **同一个宽主题** | 不能单独成为共同核心 | E2：宽主题四舍五入，硬门触发 |
| **同属一份文档 / 一起被写下来** | 不能阻止切分 | E9 结果 5：两名标注者明确对立，判准分歧 |
| 「同一天 / 同一人 / 同一条记录」 | **只能作背景，正反都不能用** | 否则规则失去停止能力 |

> **这一整行是本组测试最一致的发现：容器无处不在，而且模型天然倾向把容器当核心。**

### 19.3 结构与元数据类线索

| 线索 | 结论 | 依据 |
|---|---|---|
| 空行 / 换行 | **不作边界证据** | E9：2:1 独立否定；只有代码会被它误导。详见 §18.1 |
| 整套结构脚本切分（换行/空行/句号/字数） | **降级为观察坐标，不再产生边界** | §18.1：在这个语料上**两个方向同时错**——该切的不动、不该切的动 |
| 标题 / 编号 | 弱证据，且不够用 | E9：需第二层判据区分「独立条目」与「同一行为内的列举」；某条编号材料上三人分歧最大（1/10/1） |
| 宿主标签 | 降级 | ①与正文**同一个写手**，不能当独立判卷人；②记录被切开后每段继承整条标签，而切开的理由恰恰是「这条讲了几件不相干的事」 |
| 记录长度 | 只能证明「无需检查」 | E5：不需要切的记录中有若干比「最短的需要切的」还长 |
| 手写显式引用 | 加分通道，非主干 | E6：覆盖率 0.4% |

### 19.4 契约与评测类

| 做法 | 结论 | 依据 |
|---|---|---|
| 要求模型逐字复制证据原文 | 改为只给位置 | E12：引号规范化即可击穿 |
| 指望模型自己使用「不确定」 | 不可依赖 | E11 + E12：0/4，可复现 |
| 把「弃权率下降」读成改进 | 禁止 | E3：新候选正是靠一律弃权在负例上「变好」 |
| 把「全部判有核心」当默认 | 禁止 | E3：旧提示词的地板效应；它把弃权在语用上写成了任务失败 |
| 用「一致率」验收切分 | 失效 | **「从不切」与「全都切」各自与自身永远一致**，任何一致率指标都打不倒（§18.2） |
| 跨提示版本比较切分率 | 无意义 | §18.2：提示词那一端才是主开关；脱离它的切分率读不出模型能力 |
| 自动生成完备配对当测试集 | 拒绝 | E10：66 个自动配对里绝大多数只反复测「无关」 |
| 用被测检索自己产出金标准 | 禁止 | 循环证明 |
| 候选池包含 query 写入之后的记录 | 禁止 | 分数虚高且不可部署 |
| 把语义分歧与证据拒绝合成一个数 | 禁止 | E11：6/10 与 5/10 的差**全部**来自证据契约 |
| 用单次运行的分数比较两个配置 | 禁止 | E14：同合同下联合稳定 0/10，±1 与噪声不可分。改用**同期配对 A/B** + 机制/危害指标 |
| 把「校验器全部通过」读成判断自洽 | 禁止 | E14：40 个观测里 17 个非 canonical 配对，其中 **10 个逻辑互斥**，全部放行 |
| 靠提示词里的禁令约束模型 | 无效 | E15：提示词明写「同名本身不是证据」，模型仍只拿一个人名当证据，校验器放行，假边落库 |
| 把连通分量当最终产品形态 | 否决 | E16：局部边各自合理 → 投影出的块越来越大，**糊化来自传递，不是判错** |
| 以「找全真实关系」为目标 | 否决 | E16：人工样张自陈约四分之一未画出；找全既不现实，也未被证明对追溯有用 |
| 把「完整的连接关系」当成存在的金标准 | 否决 | E16：人在同一批 12 条上也无法把它们全部明显关联起来，弱联系是**有意留空**的——完整版在人这一侧就不存在 | **一般** |
| 同时要「保留弱边」和「集合可分」 | 否决 | E16：集合之间必须允许重叠 → 不能靠单属关系断开传递；弱边一留，任意两区域间都存在若干说得通的弱边，**没有干净的分割位置** | **一般** |
| 基础设施失败后就地改提示词重跑 | 禁止 | E3：改进/回归记为「不可评估」，不是「测得 0」 |
| 看到输出之后再改通过门 | 禁止 | E3：事后认为某门过严，只能标注它无效，不能回改历史结果 |

### 19.5 一条必须记入的负面证据（来自外部）

多项系统性评测一致指出：**语义切分的计算成本并不总能换来端到端收益**；固定长度／递归 + 合适 overlap 在不少场景持平甚至更优；语义切分在**检索指标**上好看，在**最终答案准确率**上常被追平。

**正确的落点**（见 §1.2）：这些评测针对的是**检索单元**，因此不构成对「按语义找人可读单元」的直接反驳。

**但它删掉了一个动机**：

> **「语义切分更好」这个前提，在认真测过的地方没有站住。不能再拿它当理由。**

而且它暴露一个更大的洞：**这一线工作全部以下游检索/问答为评测口径，没有一项测量「切得好对读的人是否更有用」。** 本组测试同样一次都没测过。

---

# 20. 通用方法学教训

这一节可能比结论更有用，因为它不依赖语料。

## 20.1 九个具体错误

按发生顺序，每条附「它长什么样」，因为下次它不会以同样的面孔出现。

1. **把语料的特殊情况写进通用设计——三次。**（把标签当可用；把手写显式引用当可能的主干；把「三个时代」写成流水线第 0 步。）
   **形状**：这个库有的东西，被默认成所有库都有。

2. **把一个既有约定写得像本轮新证明出来的。**
   **形状**：证据来源被抬升。

3. **「改正」出一个不存在的节省。** 把「每条都要调模型」改成「绝大多数记录零额外调用」——可那一遍无论如何都要跑。
   **形状**：读起来更省钱，实际不省。**比原来的错更难发现，因为它听上去像改进。**

4. **只按条数下结论。** 说「切分基本不是问题」，但按文字量算，一成的长记录装着一半以上的文字。
   **形状**：选了一个让结论更干净的计量口径。

5. **把一个显示层的旋钮说成语义的刻度。** 把四种关系类型说成「从严到松」，滑到最松会合并**已经被正面判定为不同事件**的对。
   **形状**：为了让概念好讲，把结构压平了。

6. **同一个错在实现侧又犯一次。** 第 5 条认过之后，又提了对全部关系类型做连通分量。
   **形状**：概念上改对了，代码里没跟着改。

7. **提议一个测试，但它不改变任何当前决定。** 被问「它会改变哪一个实现决定」，答案是一个也不改。
   **形状**：看起来严谨，实际是给还没跑的东西建验收委员会。

8. **把三件不同的事混成一个失败率报出去**（确定失效 / 不确定 / 依赖一条还没定义的规则）。
   **形状**：一个数字，读不出意思。

9. **把别人的一句话读成支持自己的证据。** 把标注者「懒得回去翻选项」读成「选项装不下」。
   **形状**：证据方向刚好指向想要的结论时，没有再问一句。

**共同点**：这九条里没有一条是「算错了」。**全部是把某种东西说得比它实际更强**——更通用、更省钱、更干净、更收敛。检查自己产出时，找的就是这个。

## 20.2 七条可直接复用的实验纪律

1. **金标准先封存，并连同事前预测一起封存。** 预测的命中率本身是一个诊断：E7 的四条预测错了三条，且系统性地把分歧预测在了错的位置——这个信息只有事前写下来才拿得到。

2. **卡片与答案分文件保存，并写一条测试断言答案不在卡片文件里。** 这比「记得别看」可靠。

3. **通过门在第一次调用前提交，看到输出后不得修改。** 事后觉得某道门不合适，只能**标注它无效**，不能回改历史结果（E3 有两处这样处理）。

4. **把两条退化策略用测试堵死**：「全部弃权」和「全部接受」都必须**算不出高分**。否则一个只会说同一句话的系统会通过评测——E3 的新候选正是靠一律弃权在负例上「变好」的。

5. **出题者的判断要单独打折，并主动披露污染。** E8、E9 里所有异常（三处交叉、判准不一致、使用了被禁止的合并理由）**全部集中在出题者一侧**。若不把出题者单列，这些会被平均掉，变成「大家都有点分歧」。

6. **语义失败与契约/基础设施失败必须分列。** 否则一次引号规范化会被读成模型不会判关系；一次 HTTP 400 会被读成方案不可行。E3 的做法是把不可评估的东西记成 `NOT_EVALUABLE`，**而不是测得的 0**。

7. **严格性与「决策错了的代价」成正比，而不是与好奇心成正比。** 跑一次几毛钱、错了重跑即可的东西，不该先建论文级验收体系。本组测试中**两份写得非常完整的验收合同因此被主动降级**为「撞上了再翻回来看」的注意事项清单。

## 20.3 一个反复出现的失败模式，值得单独命名

> **分不出「做对了」和「它总是这么答」。**

它至少出现了五次，每次面孔都不同：

| 出现处 | 形状 |
|---|---|
| E1 附带（B1） | 闸门**算术上不可能通过**，所以永远读到「贴着基线」 |
| E3（旧提示词） | **从不弃权**，于是在正例上永远「对」 |
| E3（新候选） | **一律弃权**，于是在负例上永远「对」 |
| E8（遮名比率） | 分母与判据都没定义，数字却看似可读 |
| E9（嵌套断言） | 只记「切在哪」不记「哪里不能切」，故未交叉的配对不可证伪 |

**每一次的解药都是同一个：先问「什么样的结果会让这个结论为假」。**

## 20.4 一条关于「往回退」的经验

E2 结束时最有价值的产出不是一个更好的提示词，而是一句判断：

> **核心 ↔ 边缘 才是主要问题；应当退回上一层去减少宽主题簇，而不是继续在这一层加反例。**

九版迭代之后，最后两版之间只剩 2 处翻转，且都没走到位——**这是收益递减的明确信号**。

**当一层的改动开始只产生微小且方向正确但不到位的变化时，问题多半在它的输入。**

---

# 21. 证据分级与总限制

## 21.1 分级

本文每条主张属于以下四档之一，**四档不可互相冒充**：

| 档 | 含义 | 例 |
|---|---|---|
| **语料观察** | 在这一个真实语料中已观察到 | 显式引用 0.4%；零词面重合的改名；持续状态类材料；27 配对的嵌套分布 |
| **合成材料上的行为** | 在刻意构造的材料上观察到 | E3 的容器诱饵抵抗力；E4 的管道校准 |
| **维护者观察** | 实际使用中反复看到，**未量化、无对照** | §18.2 提示词把切分推向两个极端 |
| **外部支持** | 论文/公开系统支持某机制，**未在本语料验证** | 多粒度表示、候选生成与判定分离、跨文档事件共指的负例设计 |
| **工程假设** | 实现假设，需要真实运行数据确认 | 各路 top-5 预算、候选批量约 12 |

> **合成材料上的行为不能外推到真实材料。** E3 证明的是「模型能不能抵抗**预先设计好的**容器诱饵」，而 E2 在真实材料上的失败形状要难看得多。

外部材料另有一套可靠度分级（详见《参考方案与文献》）：

```text
已核  >  他方核实  >  二手未核  >  社区风向（无对照实验）
```

## 21.2 总限制

1. **单一语料、单一作者、单一语言。** 所有「在这个库上不成立」的结论都不能外推成一般规律。
2. **样本量小。** 除两项全库统计外，最大的一项是 60 张卡 / 12 组；人工标注部分是个位数。
3. **标注者最多 3 人，其中 1 人始终是出题者。** 独立性在 E8 之后已受损（答卷曾直接交与第三位复核者），需要重建。
4. **材料体裁偏工程类**（日期、版本号、轮次、名称等锚点相对密集）。「非名称通道存在」这一结论**可能只在该体裁成立**。
5. **单一 provider 家族、单一提示版本序列**，没有跨模型对照。
6. **切分能力从未被验证**——真实样本 0 次切分，且尚未做定向压力测试。
7. **候选召回除显式引用外一条都没量过。**
8. **「这套东西对读的人是否更有用」测过一次（E16），结论是价值混合**——机器确实找到了人工未画出、但回看被认可的联系；但连通投影的糊化与「信息量仍然过大」未被解决，选择性追溯不成立。**一批 12 条、一名人工评审，且该批材料偏反思、几乎没有事件可串。**
9. **重复采样只做过一轮（E14）**，且只在一个合同上。轮次之间存在整轮相关，因此有效样本量小于观测数。
10. **上一版报告有四处结论需要按 §0.0 更正**——这说明**只读归档文档会系统性丢掉「这个数是在什么条件下测的」**。

---

## 附：配套材料

- 《实验记录补遗》（S1–S11）——未单列的中间候选、执行中止、合同审计、离线复算，以及线索消融与两条横向观察；
- 《参考方案与文献》——正文引用的，以及查阅过但正文未引用的；
- `reference-code/` —— 脱敏后的统计脚本与参考实现。
