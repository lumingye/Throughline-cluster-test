# 参考方案与文献

> 配套《个人记忆聚合：实验记录》。  
> 分两部分：**A. 正文引用的**（某项测试的设计或某条否决以它为依据）；**B. 查阅过、参与了排除或收窄，但正文未直接引用的**。  
> 每条记：**机制摘要 / 可核参数 / 可靠度等级 / 该工作自身的适用边界**。
>
> **本文不写「我们借什么、不借什么」**——那取决于各自的目标。这里只提供：**它做了什么、证据有多强、它自己的前提是什么。** 是否适用请自行判断。

---

## 0. 可靠度分级

```text
【已核】      直接检索/阅读一手来源确认
【他方核实】  由另一位复核者核实过发表状态与机制
【二手未核】  来自转述，写进任何工单前必须回一手核对
【社区风向】  实践社区经验，无对照实验；给方向与失败模式，不给证据
【产品文档】  官方产品/API 文档；是工程先例，不是同行评审证据
```

**四档不可混用。** 特别是【社区风向】与【二手未核】里的任何数字，都不得作为结论引用。

## 0.1 一条贯穿全部外部材料的方法学警告

本清单里绝大多数工作，其共同前提是：

> **同一事件会被多方独立记述，且记述中含可对齐的事实锚点**（时间、地点、人名、数字、机构、版本号）。

单人书写的个人记忆库前提**相反**：单一作者、共同背景大量位于文本之外、锚点稀疏、中位长度极短。

因此在这类语料上，外部材料的价值排序通常是：

```text
评测方法与数据集设计  >  架构分解（候选生成 / 判定分离）  >  具体算法
```

**具体算法排在最后。**

---

# A. 正文引用的

## A.1 长期记忆 / 层级组织类系统

### RAPTOR — 递归聚类、摘要、再聚类

- 论文：https://arxiv.org/abs/2401.18059
- 代码：https://github.com/parthsarthi03/raptor
- 等级：【已核】（官方代码可读到默认参数）

**机制**：

```text
文档 → 连续文本块 → embedding → UMAP 降维
     → 高斯混合软聚类 → LLM 生成簇摘要
     → 摘要重新 embedding → 继续递归
```

文本块与上层摘要都作为节点，用同一种「嵌入—聚类—摘要」的递归生成不同抽象层级。

**官方实现的默认值**（属实现默认，**不是适用于个人记忆的最优值**）：

| 参数 | 默认值／规则 |
|---|---|
| 随机种子 | `224` |
| UMAP 距离 | cosine |
| 全局 UMAP 邻居数 | `floor(sqrt(N-1))` |
| 局部 UMAP 邻居数 | `10` |
| 降维维数 | `10` |
| 候选簇数 | `1 .. min(50, N)-1` |
| 簇数选择 | 最低 BIC |
| 软归属阈值 | `0.1` |
| 单簇最大 token | `3500` |
| 超长簇 | 递归重聚类 |
| 单节点簇 | 直接保留，不重聚类 |

**它自己的适用边界**：目标主要是问答检索；**不要求每个父节点经受人类语义审查**。同一递归算子同时负责底层小簇与高层主题。

**与本组测试的关系**：T0 直接测的就是这条路线的前提（向量里是否存在可发现的簇结构），结果为否。

### Mnemis — 基础相似图与层级类别图分开

- 论文：https://arxiv.org/abs/2602.15313
- 代码：https://github.com/microsoft/Mnemis
- 等级：【已核】

**机制**：同时维护两张图——**基础图**（实体、事件、关系及其相似检索）与**层级图**（类别节点组成的上位语义结构，供逐层全局选择）。即「谁与谁接近」和「从哪个上位类别能找到它」**不是同一关系**。

三条层级原则：

1. **最小概念抽象**——父类别只做足够涵盖子节点的最低限度抽象，不能一层就跃升到空泛概念；
2. **多对多映射**——一个底层节点可属于多个类别，因此结构是 DAG 而非严格树；
3. **压缩效率约束**——普通类别应含若干子节点；无法自然归类的内容可单独上升；新一层不应比下一层更大；单例上升过多、无法继续压缩时停止；达到最大层数时停止。

**论文未公开**统一的最小子节点数与最大层数数值。技术文档应保留为「未公开／部署参数」，**不补猜**。

**公开度**：官方仓库公开论文、核心 prompts、全局选择代码与实验结果；**完整层级构建器并未作为可直接替换的独立包公开**。

### GAM — 先形成事件过程，再进入长期主题网络

- 论文：https://aclanthology.org/2026.acl-long.1600/
- 等级：【已核】

**机制**：

```text
持续输入 → 活跃事件图 → 语义边界／维护触发
        → 事件过程摘要 + 原文 → top-k 候选
        → LLM 判断关系和置信度 → 接入长期网络
```

关键是：边界判别**不是每轮都运行**，而是在会话结束、自然停顿、缓冲区溢出等**稀疏维护事件**触发。

**论文可核参数**：活跃事件缓冲上限 `2048 tokens`；候选召回 top-`5`；最终检索 `k=10`；索引 embedding `all-MiniLM-L6-v2`；reranker `cross-encoder/ms-marco-MiniLM-L-6-v2`；时间调制 `1.4`、角色调制 `1.4`、置信度调制 `1.2`。**连边阈值论文使用但未公开数值。** 边界阈值不使用固定数值距离，由 LLM 语义判别 prompt 代理。

**它自己的适用边界**：面向持续对话流；它的「语义边界」粒度较粗；**没有解决任意深度的上位组织**。

### HiGMem — 向量召回候选，LLM 决定真正成员

- 论文：https://aclanthology.org/2026.findings-acl.1690/
- 代码：https://github.com/ZeroLoss-Lab/HiGMem
- 等级：【已核】

**机制**：两层结构（底层保留原始对话与结构化元数据，上层保留摘要和事实表作为语义锚点）。

```text
新输入 → embedding 找出候选事件 → LLM 查看事件摘要
      → 判断哪些值得进一步读取／归属 → 只展开相关底层材料
```

一个底层单元可属于多个事件。

**这是「embedding 只做候选、语义模型做判断」最直接的外部先例之一。**

**它自己的适用边界**：底层输入是对话轮次，**本身没有认真解决长记录内部的切分问题**。

**注**：本轮整理**没有**把未逐项核验的窗口、阈值或 top-k 写入任何表。若要复用其参数，应从官方代码重新绑定具体 commit，而不是引用二手摘要。

### A-MEM — 动态卡片网络，允许单例长期存在

- 论文：https://arxiv.org/abs/2502.12110
- 代码：https://github.com/agiresearch/A-mem
- 等级：【已核】

**机制**：每条新记忆写成带上下文描述、关键词、标签、向量和链接的卡片；系统检索相关旧卡片，由模型判断是否建立有意义连接，并可更新已有卡片的上下文属性。**没有强制层级。**

严格说，它很多时候**不合**——它是网络思路，而不是「所有相关内容都必须长出父节点」。

**它自己的适用边界**：它会**演化/更新已有卡片的解释性属性**。若某个系统要求「旧解释不可被静默覆盖、必须版本化」，这一点需要另行处理。

## A.2 切分与多粒度检索

### Meta-Chunking — 逻辑连续不等于语义相似

- 论文：https://arxiv.org/abs/2410.12788
- 代码：https://github.com/IAAR-Shanghai/Meta-Chunking
- OpenReview（ICLR 2025 投稿，**withdrawn**）：https://openreview.net/forum?id=gh563RwulS
- 等级：【他方核实】—— **机制可核，结论按预印本使用**

**机制**：定义介于句子与段落之间的 `meta-chunk`，强调连续句之间的**语言逻辑关系**而不只看向量相似。两种边界方法：

- **PPL Chunking**：利用上下文条件下的困惑度分布寻找边界；
- **Margin Sampling Chunking**：让模型判断相邻内容应 `split / keep`，用两个决策 token 的概率 margin 与动态阈值作边界依据。

随后把 meta-chunks 当作不可再拆的逻辑单元，按用户指定目标长度 `L` **相邻合并**。

**它自己的适用边界**：动态合并最终仍由**目标长度**驱动，**不是在证明某个唯一「自然最佳粒度」**；主要面向离线文档 RAG；其后续补全会重写/补全 chunk。

**值得单独记的一条**：它把「逻辑有关但词面/语义相似度不高」确立为**合法的不断理由**——与本组测试 T3 中人实际写下的连贯依据（事件连续、因果、时空）方向一致。

### LGMGC — 父块保完整，子块负责检索

- arXiv：https://arxiv.org/abs/2501.09940
- ECIR 2025 proceedings：https://doi.org/10.1007/978-3-031-88714-7_33
- 等级：【他方核实】—— 正式会议短文；本轮未找到官方代码

**机制**：

1. **Logits-Guided Chunker**：在候选句边界读取 LLM 的 `[EOS]` 概率，以较高的结束倾向寻找相对自足的**父块**；
2. **Multi-Granular Chunker**：把父块再递归切成 `θ/2`、`θ/4` 等更小的**子块**。

检索时**不直接**把最小子块作为最终证据：父块的分数由其子块（含自身）最高相似度决定，最终向生成器提供得分最高的**父块**。

```text
小子块：更容易命中精确查询 → 找到父块 → 最终读取父块的完整上下文
```

**它自己的适用边界**：子块由**固定比例递归切分**，并非每层都重做语义边界判断；评价口径是 passage retrieval / QA，**不等于人类认可其每一级为真实语义单元**。

**与本组测试的关系**：T5 从三名标注者的答卷中量出的嵌套结构与它的父子形态同形——**已经有人把这种结构当作表示层做了**。

### MemGAS — 多粒度是查询时的选择问题

- arXiv：https://arxiv.org/abs/2505.19549
- ICLR 2026 OpenReview：https://openreview.net/forum?id=i2yIvZARnG
- 代码：https://github.com/Applied-Machine-Learning-Lab/ICLR2026_MemGAS
- 等级：【他方核实】—— **强证据**

**机制**：维护 session / turn / summary / keyword 四种粒度（后两者由 LLM 生成）。新记忆加入时用高斯混合把历史划分为 accept / reject 集合形成跨记忆关联；查询时对每个粒度分别计算相关性分布的**熵**，用**逆熵软路由**加权，并在关联图上用 Personalized PageRank 检索。

**它自己的适用边界**：**起点仍是 session / turn 等已有单元**，没有解决原始长文本的局部连贯切分；其 accept/reject 是关联构造，不等同于事件同一；熵路由优化的是**查询时检索选择**；摘要/关键词是派生表示。

### xMemory — 先解耦证据，再聚合

- 论文：https://arxiv.org/abs/2602.02007
- 代码：https://github.com/HU-xiaobai/xMemory
- 等级：【他方核实】—— 高质量可复现预印本

**机制**：

```text
raw messages → segments（局部连贯事件）
            → memory components（事实、约束、属性、关系、状态更新）
            → groups（高层访问单元）
```

强调 **decoupling before aggregation**：先从局部完整片段中抽出可复用、可区分的证据成分，再把成分聚成高层组。组织用一个 `sparsity + semantic faithfulness` 目标评分；新记忆到来后，过大/内部不一致的组可拆，过小或孤立的组可并，因此**层级是可修订的**。检索自顶向下，只有当更多证据能降低不确定性时才展开到原始材料。

**它自己的适用边界**：其目标函数优化的是**检索结构的平衡、连贯与扫描成本**，不是「这个父节点在人类语义上是否真实成立」；其组以**划分**为基本形式；「segment 是局部事件」仍需要它自己的边界合同。

### CLAG — 先路由到局部邻域，再只在局部演化

- 论文：https://aclanthology.org/2026.findings-acl.824/
- 代码：https://github.com/dmis-lab/CLAG
- 等级：【他方核实】—— **强证据**（有正式消融实验）

**机制**：

1. embedding 先给小模型路由器若干候选簇；
2. 路由器决定路由到已有簇或创建新簇；
3. 簇保存 profile（语义摘要 + 代表性标签）；
4. **链接生成与记忆演化只在该簇内运行**；
5. 检索先用 profile 选簇，再簇内细搜。

核心价值是把「全库比较／全库改写」缩成**局部语义邻域**，并用消融实验比较局部演化 vs 全局演化、两阶段检索 vs 全局检索。

**它自己的适用边界**：其局部演化会生成新版本**直接替换**簇内原记忆并更新 profile；其簇成员关系是**路由与检索结构**。

**旁证价值**：它的两阶段检索与实体解析的 `blocking → matching` 是同一架构，**从第三个方向独立到达**。

### Dense X Retrieval — 局部完整单元下面仍可有更细的检索原子

- 论文（EMNLP 2024 main）：https://aclanthology.org/2024.emnlp-main.845/
- 等级：【他方核实】—— **强证据**

**机制**：比较 document / passage / sentence 等检索粒度，并引入 `proposition`——文本中的**原子事实表达**，要求表达单一 factoid 且在自然语言上自包含。核心结果属于检索层：在其 dense retrieval 与给定计算预算的下游 QA 设置中，细粒度 proposition 索引优于 passage 级单元。

**它最重要的一般性含义**：**语义单元与检索单元可以不同**（与 xMemory 的 `segment → component`、LGMGC 的 `parent → child` 独立收敛到同一区别）。

**它自己的适用边界**：proposition 是**事实检索粒度**，不是事件边界金标准；叙事、因果链、关系修正与长期状态可能被原子化后丢失局部结构。

### LumberChunker — 直接寻找内容开始发生转折的位置

- 论文（Findings of EMNLP 2024）：https://aclanthology.org/2024.findings-emnlp.377/
- 等级：【他方核实】—— **强证据**

**机制**：面向长篇叙事文档，反复把一组连续段落交给 LLM，要求识别**内容从哪里开始发生 shift**，从而得到可变长度片段。论文还发布了长篇叙事检索基准。

**它比「相邻 embedding 相似度低就切」更接近人实际使用的判断**：前一部分的局部推进是否还在继续，还是这里出现了足以单独成立的内容转折？

**它自己的适用边界**：优化的是叙事检索，**不证明其边界就是个人记忆的真实事件边界**；LLM 直接判断仍受 prompt、窗口内容与模型版本影响；不解决跨记录身份、别名、持续状态。

### Late Chunking — 边界不变，改变 embedding 发生的位置

- arXiv：https://arxiv.org/abs/2409.04701
- 等级：【二手未核】（机制可核的预印本）

**机制**：

```text
长上下文整体输入 transformer → 得到带全文上下文的 token 级表示
                            → 再按既有边界 pooling → 上下文化的块向量
```

**它严格来说不是切分算法**：边界仍需由其它方法给出。它改变的是同一个块**被如何表示**。

**已知风险**：整段上下文也可能造成**上下文污染**——若容器本身混入多件事，上下文化表示可能把本应分开的单元因共同大背景**拉得更近**。

### Contextual Retrieval（Anthropic 工程方法）

- 官方页面：https://www.anthropic.com/engineering/contextual-retrieval
- 等级：【产品文档】—— **工程证据，不是同行评审论文**

**机制**：在建 embedding 与 BM25 索引前，先让模型依据全文为每个 chunk 生成约 `50–100 tokens` 的专属上下文，前置到 chunk。

**官方跨多类知识库实验**：

- Contextual Embeddings：top-20 检索失败率 5.7% → 3.7%（相对 −35%）；
- 再加 Contextual BM25：→ 2.9%（−49%）；
- 再加重排：→ 1.9%（−67%）。

**这些数字属于该工程实验设置，不应外推成长期记忆库的保证。**

**它自己的适用边界**：contextualizer 可能补错所指或背景；prompt / 模型变化会导致索引表示漂移；它不决定块边界。

### Is Semantic Chunking Worth the Computational Cost?

- 论文（Findings of NAACL 2025）：https://aclanthology.org/2025.findings-naacl.114/
- 等级：【他方核实】—— **强负面/条件性证据**

**结论**：系统比较 document retrieval、evidence retrieval 与 retrieval-based answer generation 后，**语义切分的额外计算成本没有被一致的性能增益所证明**。

**正确的使用方式**：作为**强制基线纪律**——即使某个语义/LLM 切分器看起来更聪明，也必须与固定长度、段落/结构、合理 overlap 等基线比较。

**不能扩写成**「语义切分普遍更差」；论文支持的是**收益不一致**。

**它自己的评测口径**：**检索单元**。它不测量「切得好对读的人是否更有用」——**这一线工作没有一项测量后者**。

## A.3 跨领域：同一性判定与候选生成

### 跨文档事件共指（CDEC）

- Cross-document Event Coreference Search: https://arxiv.org/pdf/2210.12654
- 数据集系统分析与统一：https://arxiv.org/pdf/2603.00621
- 等级：【已核】（链接经检索核对存在；规模数字为转述）

**任务定义**：判断分布在不同文档中的事件提及是否指向同一个真实事件。

**它覆盖的是哪一块（重要收窄）**：直接对应「事件锚点下的同一事件」子任务。**同一过程的不同阶段、同一持续状态的不同侧面、同一所指但不同次发生，都超出标准 CDEC 的任务定义。**

**标准数据集 ECB+**：982 文档 / 43 组 / 6,833 事件提及 / 26,712 共指链接。**其设计正中这类问题的痛点**：

> 每组由**两批**文档构成，讲的是**两个不同的事件**，但**用词高度相似**。

**现成的评测指标**：MUC / B³ / CEAF / CoNLL F1。

**它自己的适用边界**：**做的是新闻。** 同一事件被多家媒体报道，用词不同但**事实锚点密集**。

> **方法不能直接搬；评测方法与数据集设计可以搬。**

### 实体解析 / 记录链接

- Blocking 技术综述：https://doi.org/10.1007/s11390-020-0350-4
- (Almost) All of Entity Resolution: https://arxiv.org/pdf/2008.04443
- 等级：【已核】

**标准分解**（自 Fellegi–Sunter 1969 以来）：

```text
blocking（生成候选，避免 O(n²) 两两比较） → matching（对候选对判定是否同一）
```

「候选怎么来」在这个领域就叫 **blocking**，有五十年积累与完整工程实践。

**一个与无监督场景相合的性质**：Fellegi–Sunter 框架**不需要训练数据**——参数由 EM 直接在候选对集合上估计。

**它自己的适用边界**：处理的是**结构化字段**（姓名、地址、生日）。自由文本没有可对齐字段，**具体的键构造方法不可直接用**；但**分层架构与代价分析可用**。

### 分割评测的指标线

- 等级：【已核】（直接检索确认存在）

| 方向 | 内容 |
|---|---|
| **Pk / WindowDiff** | 经典惩罚型指标，对「差一点点」的边界给部分分。已知偏差：**偏向边界少或边界密集的切法** |
| **Boundary Similarity / Segmentation Similarity / Boundary Edit Distance** | 针对窗口型指标缺陷提出，处理近似边界。有开源实现（SegEval；该包较旧，构建可能失败） |
| **粒度感知评测** | 已有以此为题的工作（对话分割方向） |
| **层次化评测** | 用动态规划把参考的每一层与假设的**非递减**层对齐（monotonic alignment）；另有多层 boundary similarity 扩展 |

**「非递减对齐」就是「粗切点集合 ⊆ 细切点集合」的形式化版本。** T5 从标注数据中量出的结构，在该线工作中是**建模前提**——不必自行发明度量。

**该线工作自陈的局限**（恰好是 T5 撞上的情形）：

> 这些指标都是**对着单一参考切分**计算的——尽管多种切分与多种粒度都可能站得住。

## A.4 产品先例

### Nowledge Mem

- Memories：https://mem.nowledge.co/docs/memories
- Threads：https://mem.nowledge.co/zh/docs/zh/threads
- Search architecture：https://mem.nowledge.co/docs/concepts/search-architecture
- Memory Links：https://mem.nowledge.co/ko/docs/concepts/memory-links
- EVOLVES：https://mem.nowledge.co/es/docs/concepts/evolves
- Knowledge Graph：https://mem.nowledge.co/docs/knowledge-graph
- Crystals：https://mem.nowledge.co/docs/concepts/crystals
- Memory decay：https://mem.nowledge.co/docs/concepts/memory-decay
- 等级：【产品文档】—— 不是论文评测，也不是对内部实现的审计

**它把几个常被混成一个的动作，在产品层做成了不同对象：**

```text
Thread / Source     原始过程
Memory / Unit       长期可独立阅读的结论（带 source_thread / source_range 出处）
Search              六路并行（semantic / full-text / entity / community / label / graph traversal）
EVOLVES             版本/演化关系
Memory Link         「因为某个理由应该一起读」的开放关系边（带 relation reason）
Louvain community   主题社区 / 可视化
Crystal             多来源收敛后的综合知识
```

几处可核细节：

- **提取**：一条长来源可以产生多个长期单元；提取时读取全文上下文，并可传入已有单元作为关系判断上下文；有 worthiness gate。
- **演化关系**分两类：`Progression: replaces / enriches`、`Validation: confirms / challenges`。`replaces` 把旧版本标记为 superseded 但**历史仍保留**；`confirms / challenges` 不替换原对象。检测流程是「语义足够接近 → **再评估具体关系类型**」，而不是 similarity → merge。
- **社区**：知识图谱明确运行 **Louvain 社区发现**，用于主题结构呈现与社区检索。**官方没有把社区描述成同一事件或正式语义父节点。**
- **综合节点（Crystal）**：门槛是至少 **3 条独立来源**、触及同一主题、有足够不同的信息贡献、综合需通过质量评估、生成后指回每个来源。首次生成为 unreviewed，用户可 confirm / dismiss / edit；来源被更新后 Crystal **不会被静默重写**，而是标 stale 并提出重评估。
- **衰减/置信**：衰减用 recency + 访问频率（对数）；置信度另含搜索出现、显式点击、阅读时间、演化边、综合成员，并有重要性下限。

**它自己的适用边界与三处不可直接推广**：

1. 其智能提取会检查重复并可能 **update 已有条目**——这适合减少知识库噪声，但**「重复的语义内容」与「再次表达这个动作」不是同一件事**；
2. 其搜索最终以语义相关性为主导并混合若干排序信号——**检索排序的权重不应越权成为「谁该合」的规则**；
3. 「至少 3 条独立来源」是**产品规则，不是普遍认知规律**。

另外注意区分三种不同的频率信号：**访问频率 ≠ 内容中主动重复表达的频率 ≠ 重要性 ≠ 置信度。**

## A.5 实践社区（PKM / Zettelkasten）

- 等级：【社区风向】—— **设计经验与失败模式，不是证据。** 该社区没有对照实验。

**为什么值得看**：RAG / agent-memory 那一圈解决的是**机器为查询做检索**；这条脉络解决的是**人组织自己的记录**。

**引用的四点**：

**① 原子性的定义。**

> **原子性指「一条只讲一件事」，不是「一条要短」。它是相对当前用途而言的，不是客观最小单位——同一内容对不同人可以是不同粒度。**

T5 中三名标注者在同一材料上给出不同深度且全部站得住。**该社区把这件事当作设计前提，本组测试是量出来的。**

争议两端也与实测撞上的一致：太碎 → 链接与结构笔记变成负担，碎片漂浮，维护成本爆炸；太整 → 失去可重组性，退回长文档。

**② 一条与「能合不能拆」相反的默认。**

社区的操作规则是「**宁可先偏大，之后回顾时再拆**」。而「能合不能拆」这条常被当作通用原则的说法，在两件事上指向**相反**的默认：

| 对象 | 安全方向 | 理由 |
|---|---|---|
| **名称／别名** | **细优先**（保留各自写法，不归一） | 合并后原写法不可恢复 |
| **切分粒度** | **粗优先**（先大，需要时再拆） | 切碎后「这些原本在一起」的信息不可恢复，且过碎有持续维护成本 |

**③ 一条可操作的原子性判据**：能不能用**一句陈述句标题概括、一眼看懂**。便宜、可核、可写进 prompt。

**④ 一条明确的反对意见，必须记为挑战而不是支持。**

该传统的**核心立场**是「**不该让系统替你归类**」，其论证是：手工链接记录的是「你当时为什么觉得这两点有关」——这是独特的思维痕迹；搜索/相似度是确定性、可重复的，但**丢失了你的判断**；自动归类会把「思考工具」变回「归档工具」。

**他们的答案是干脆不自动化。** 任何主张「自动提案 + 人可拒绝 ≠ 替你归类」的系统，都应把这条记为**未验证的挑战**。

**可直接复用的失败模式清单**（数字慎用，均为单团队、单语料、无对照的复盘）：

- **Overchunking**：切太碎 → 检索到一堆碎片，上下文不完整；
- **跨引用断裂**：条款写「见第 4.1 节／附录 C」，而被引内容不在同一块；
- **结构体被拦腰切开**：表格、列表、代码块；
- **数值/日期被切开**：日期的月日与年份分属两块，模型补错年份。

---

# B. 查阅过、参与了排除或收窄，但正文未直接引用的

列在这里是为了让复核者能判断：**上面是不是只挑了支持自己的材料。**

## B.1 记忆系统（作横向比较）

| 系统 | 链接 | 等级 | 机制与它自己的边界 |
|---|---|---|---|
| **SimpleMem** | https://arxiv.org/abs/2601.02553 · https://github.com/aiming-lab/SimpleMem | 【已核】 | 把对话压成可独立理解的原子记忆，建多个互补索引视图，再异步整合相关单元并按查询复杂度动态控制检索范围。**边界**：整合方向是把相关记忆压缩成单一高密度条目——成员差异、矛盾与来源会被压掉 |
| **H-MEM** | https://aclanthology.org/2026.eacl-long.15/ | 【已核】 | 多层语义抽象节点，高层向量携带指向下一层的位置索引；查询从高层逐级路由，而非全库平铺相似搜索。**边界**：语义父级仍由统一相似度决定 |
| **HiMem** | https://arxiv.org/abs/2601.06377 · https://github.com/jojopdq/HiMem | 【已核】 | 分 Episode Memory（由事件/惊奇度切分建立）与 Note Memory（较稳定知识），两者用语义关系连接。**含义**：不同类型的记忆不必共用同一套聚合规则 |
| **Bi-Mem** | https://arxiv.org/abs/2601.06490 | 【二手未核】 | 事实按向量相似连图，再做社区发现，社区总结成场景。引用它主要是因为它**公开指出聚类可能放大对话噪声与记忆幻觉**——一条方向上有利的反面证据，**但仍未核** |

## B.2 切分与检索（次级参考 / 反面证据）

| 工作 | 链接 | 等级 | 内容与未进正文的原因 |
|---|---|---|---|
| **TreeSeg** | https://arxiv.org/abs/2407.12028 | 【二手未核】 | 用现成 embedding + divisive clustering 对长转写递归产生**二叉分割树**；直接面对「参考分段数本身难以确定」这个问题。**二叉递归是算法结构，不应外推为语义本体** |
| **Max–Min Semantic Chunking** | https://doi.org/10.1007/s10791-025-09638-7 | 【他方核实】 | 正式期刊论文。以语义相似度 + Max–Min 动态组句，用 AMI 与多选 QA 评估。适合作为「比简单阈值更复杂的相似度驱动切分器」基线，**但没有提供新的身份/层级关系模型** |
| **36 种切分策略跨域评测** | https://arxiv.org/abs/2603.06976 | 【二手未核】 | 36 策略 × 6 领域 × 5 embedding，以 LLM graded relevance 的 nDCG@5 为主指标。content-aware 方法总体明显优于最朴素固定字符基线，**但不同领域最佳方法明显变化**。与前述负面结果**不矛盾**，共同支持：切分收益取决于 `task × corpus × embedder × metric × cost`，**不存在可脱离场景冻结的万能切法** |
| **PSC / MFC（领域感知语义分割）** | https://arxiv.org/abs/2512.00367 | 【二手未核】 | 原转述称「域依赖强、跨域需重训」，**核对后证据不足**——作者反而报告了若干跨域结果。更准确的记录是：训练与最强证据高度集中于生物医学；作者报告跨域泛化，但仍是预印本结论。**保留为次级参考** |
| **Parent-Child / Small-to-Big** | 工程模式（如 `ParentDocumentRetriever`） | 【社区风向】 | 「小块负责精确检索、大块负责返回完整上下文」，与 LGMGC 的研究版同形。**不单独升级为研究证据**，只记录为可落地的工程对应物 |

## B.3 跨文档共指与评测方法学

| 工作 | 链接 | 为什么读它 |
|---|---|---|
| Realistic Evaluation Principles for Cross-document Coreference Resolution | https://arxiv.org/pdf/2106.04192 | 该领域自己对「评测怎么做才现实」的讨论；对泄漏与过拟合的处理值得借鉴 |
| Streamlining Cross-Document Coreference Resolution | https://arxiv.org/pdf/2009.11032 | 评测与建模的简化路线 |
| **FCC**（Football Coreference Corpus） | 见上述数据集统一工作 | 451 文档 / 3,563 事件提及 / 145,272 链接。**规模数字来自转述，未回一手核对** |
| **SegEval** | 开源实现 | 分割相似度指标的参考实现；**本环境构建失败**，故未实际使用 |

---

## C. 引用纪律

1. 只有**从论文或官方代码明确核到**的数字才列为外部参数；
2. **未公开的阈值明确标为未公开，不补猜**；
3. 任何**组合建议属于综合推导**，不代表上述任何项目的原始主张；
4. 正式工单应绑定**具体源码 commit 与实验输入**，不直接把研究笔记当作实现合同；
5. 【社区风向】给的是**方向与失败模式，不是证据**；
6. 任何【二手未核】的数字，**写进任何工单前必须回到一手来源**。
